# 🏗️ Аналіз Архітектури Оркестратора ATLAS4

**Дата**: 2025-11-19  
**Версія**: 1.0  
**Статус**: ✅ Завершено  

---

## 📊 Огляд Архітектури

### Рівні Абстракції

```
┌─────────────────────────────────────────────────────────┐
│                   CLIENT LAYER                          │
│              (Web, Mobile, CLI, API)                    │
└────────────────────┬────────────────────────────────────┘
                     │
┌─────────────────────▼────────────────────────────────────┐
│              API LAYER (Express)                         │
│  - HTTP Routes (health, chat, web, cascade, eternity)   │
│  - WebSocket Manager                                    │
│  - Error Handling & CORS                                │
└────────────────────┬────────────────────────────────────┘
                     │
┌─────────────────────▼────────────────────────────────────┐
│          ORCHESTRATOR CORE (Application)                │
│  - Lifecycle Management (init, start, stop)             │
│  - DI Container                                         │
│  - Service Registry                                     │
│  - Configuration Management                             │
└────────────────────┬────────────────────────────────────┘
                     │
┌─────────────────────▼────────────────────────────────────┐
│        WORKFLOW ENGINE (Main Orchestrator)              │
│  - TodoBuilder (Phase 1)                                │
│  - TodoExecutor (Phase 2)                               │
│  - ToolPlanner (Phase 3)                                │
│  - ToolExecutor (Phase 4)                               │
└────────────────────┬────────────────────────────────────┘
                     │
┌─────────────────────▼────────────────────────────────────┐
│           AI & MCP LAYER                                │
│  - LLM Client (Claude, GPT, etc.)                       │
│  - MCP Manager (Model Context Protocol)                 │
│  - TetyanaToolSystem (Tool Management)                  │
│  - Tool Dispatcher & Executor                           │
└────────────────────┬────────────────────────────────────┘
                     │
┌─────────────────────▼────────────────────────────────────┐
│         SERVICES & UTILITIES                            │
│  - Logger, Config, Sessions                             │
│  - WebSocket Manager, TTS Sync                          │
│  - HTTP Client, Path Utils                              │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 Ключові Компоненти

### 1. **Bootstrap Layer** (`server.js`)
```javascript
// Entry point для Node.js процесу
// Обробляє сигнали (SIGINT, SIGTERM)
// Запускає Application
```

**Відповідальність**: Мінімальна ініціалізація, обробка помилок

**Залежності**: Application

---

### 2. **Application Lifecycle** (`core/application.js`)
```javascript
class Application {
  - initializeServices()      // Реєстрація сервісів
  - initializeConfig()        // Валідація конфігурації
  - start()                   // Запуск сервера
  - stop()                    // Зупинка сервера
}
```

**Відповідальність**: Управління життєвим циклом додатку

**Залежності**: DIContainer, ServiceRegistry, Config

---

### 3. **Dependency Injection Container** (`core/di-container.js`)
```javascript
class DIContainer {
  - register(name, factory)   // Реєстрація сервісу
  - singleton(name, factory)  // Singleton реєстрація
  - resolve(name)             // Резолвування залежностей
  - initialize()              // Ініціалізація (onInit hooks)
  - start()                   // Запуск (onStart hooks)
  - stop()                    // Зупинка (onStop hooks)
}
```

**Відповідальність**: Управління залежностями, lifecycle hooks

**Залежності**: Жодних (базовий клас)

---

### 4. **Service Registry** (`core/service-registry.js`)
```javascript
registerAllServices(container)
├── registerCoreServices()
├── registerApiServices()
├── registerStateServices()
├── registerUtilityServices()
├── registerOptimizationServices()
├── registerMCPWorkflowServices()
├── registerMCPProcessors()
└── registerWorkflowModules()
```

**Відповідальність**: Централізована реєстрація всіх сервісів

**Залежності**: DIContainer

---

### 5. **Workflow Engine** (`workflow/core/workflow-engine.js`)
```javascript
class WorkflowEngine {
  - execute(userMessage, session)
    ├── todoBuilder.build()      // Фаза 1: Побудова
    ├── todoExecutor.execute()   // Фаза 2-4: Виконання
    └── return results
}
```

**Відповідальність**: Оркестрація workflow від запиту до результату

**Залежності**: TodoBuilder, TodoExecutor, Logger, WSManager

---

### 6. **MCP Manager** (`ai/mcp-manager.js`)
```javascript
class MCPManager {
  - initialize()              // Запуск MCP серверів
  - listTools()               // Список доступних інструментів
  - callTool(name, args)      // Виклик інструменту
  - shutdown()                // Зупинка серверів
}
```

**Відповідальність**: Управління Model Context Protocol серверами

**Залежності**: Config, Logger

---

### 7. **Tetyana Tool System** (`ai/tetyana-tool-system.js`)
```javascript
class TetyanaToolSystem {
  - initialize()              // Завантаження розширень
  - selectTools(task)         // Вибір інструментів для задачі
  - validateTools(tools)      // Валідація інструментів
  - getStatistics()           // Статистика інструментів
}
```

**Відповідальність**: Управління інструментами з LLM валідацією

**Залежності**: MCPManager, LLMClient

---

### 8. **MCP TODO Manager** (`workflow/mcp-todo-manager.js`)
```javascript
class MCPTodoManager {
  - buildTodo(userMessage)    // Побудова TODO
  - executeTodo(todo)         // Виконання TODO
  - validateResults()         // Валідація результатів
  - replan(todo)              // Перепланування
}
```

**Відповідальність**: Управління TODO workflow з MCP інтеграцією

**Залежності**: MCPManager, LLMClient, TTSSyncManager

---

## 🔄 Потік Виконання

### Типовий Workflow

```
1. Client Request
   ↓
2. Express Route Handler
   ↓
3. WorkflowEngine.execute(userMessage, session)
   ↓
4. TodoBuilder.build()
   ├─ Parse user message
   ├─ Analyze intent
   └─ Create TODO items
   ↓
5. TodoExecutor.execute()
   ├─ ToolPlanner.plan()
   │  ├─ Select tools
   │  └─ Create execution plan
   ├─ ToolExecutor.execute()
   │  ├─ Call MCP tools
   │  ├─ Validate results
   │  └─ Handle errors
   └─ Verification
   ↓
6. Return Results
   ├─ Success response
   ├─ Metrics
   └─ Logs
```

---

## 📦 Залежності Між Модулями

### Core Dependencies
```
server.js
  ↓
Application
  ├─ DIContainer
  ├─ ServiceRegistry
  └─ Config
      ↓
  WorkflowEngine
      ├─ TodoBuilder
      ├─ TodoExecutor
      ├─ Logger
      └─ WSManager
          ↓
      MCPManager
          ├─ Config
          └─ Logger
              ↓
      TetyanaToolSystem
          ├─ MCPManager
          └─ LLMClient
              ↓
      MCPTodoManager
          ├─ MCPManager
          ├─ LLMClient
          ├─ TTSSyncManager
          └─ WSManager
```

### External Dependencies
```
Node.js Core
├─ fs (48 uses)
├─ path (48 uses)
├─ events (51 uses)
├─ url (18 uses)
├─ child_process (12 uses)
├─ util (9 uses)
├─ crypto (6 uses)
├─ http (3 uses)
└─ https (3 uses)

NPM Packages
├─ axios (52 uses) - HTTP client
├─ express (9 uses) - Web framework
├─ dotenv (6 uses) - Config management
├─ winston (3 uses) - Logging
├─ cors (3 uses) - CORS middleware
└─ @modelcontextprotocol (9 uses) - MCP protocol
```

---

## 🎯 Точки Входу (Entry Points)

### 1. **HTTP API**
```
GET  /health              → Health check
POST /chat                → Chat endpoint
POST /web                 → Web integration
POST /cascade             → Cascade integration
POST /eternity            → Eternity integration
```

### 2. **WebSocket**
```
ws://localhost:PORT/ws    → Real-time updates
```

### 3. **CLI**
```
npm start                 → Start server
npm test                  → Run tests
npm run lint              → Lint code
```

---

## 🔴 Проблемні Області

### 1. **Мертвий Код** (3,950 функцій)
**Файл**: `mcp-todo-manager.js`
- Невикористані змінні: `openBraces`, `closeBraces`, `openBrackets`, `closeBrackets`
- Невикористані функції: `isPythonModuleTask`, `isCreateModuleTool`
- Мертві гілки коду

**Вплив**: Складність, розмір файлу, конфузія

**Рішення**: Видалити всі невикористані елементи

---

### 2. **Архівні Файли** (~100+ файлів)
**Локації**:
- `archive/legacy-orchestrator-2025-10-20/`
- `backups/20251114-135805/orchestrator/`
- `web/.archive/`

**Вплив**: Розмір репозиторію, конфузія

**Рішення**: Переместити в окремий архів або видалити

---

### 3. **Залежність від Child Process** (12 uses)
**Файли**: `mcp-manager.js`, `tool-executor.js`

**Вплив**: Безпека, продуктивність

**Рішення**: Розглянути альтернативи (worker threads, native modules)

---

### 4. **Великі Файли**
**mcp-todo-manager.js**: 4,000+ рядків

**Вплив**: Складність, тестування

**Рішення**: Розділити на підмодулі

---

## ✅ Рекомендації

### Короткострокові (Тиждень 1)
1. **Видалити мертвий код** з основних модулів
2. **Архівувати** старі файли
3. **Оптимізувати** залежності

### Середньострокові (Місяць 1)
1. **Розділити** великі файли
2. **Централізувати** конфігурацію
3. **Додати** unit тести

### Довгострокові (Квартал 1)
1. **Мікросервіси** для AI/MCP
2. **Event-driven** архітектура
3. **Кешування** результатів

---

## 📈 Метрики Якості

### Поточні Метрики
| Метрика             | Значення         | Статус     |
| ------------------- | ---------------- | ---------- |
| Циклічні залежності | 0                | ✅ Добре    |
| Мертвий код         | 3,950            | ❌ Погано   |
| Архівні файли       | 100+             | ❌ Погано   |
| Середня складність  | 3.7 імпорти/файл | ⚠️ Середньо |
| Тестове покриття    | ?                | ❓ Невідомо |

### Цільові Метрики
| Метрика             | Ціль | Поточна |
| ------------------- | ---- | ------- |
| Циклічні залежності | 0    | 0 ✅     |
| Мертвий код         | <500 | 3,950 ❌ |
| Архівні файли       | 0    | 100+ ❌  |
| Середня складність  | <3.0 | 3.7 ⚠️   |
| Тестове покриття    | >80% | ? ❓     |

---

## 🚀 Наступні Кроки

1. **Запустити чистку** за планом `ORCHESTRATOR_CLEANUP_PLAN.md`
2. **Перевірити** всі тести
3. **Оновити** документацію
4. **Розпочати** рефакторинг великих файлів

---

**Статус**: ✅ Готово до дій  
**Автор**: Cascade AI + MCP Codemap  
**Дата оновлення**: 2025-11-19 01:58 UTC+02:00
