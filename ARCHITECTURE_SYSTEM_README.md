# 🏗️ Архітектурна Система Windsurf

**Версія**: 1.0.0  
**Статус**: ✅ ГОТОВО  
**Дата**: 22 листопада 2025

---

## 📌 Короткий огляд

Архітектурна система — це **автоматичний моніторинг** кодової бази, який:

- 🔍 **Аналізує** архітектуру **всього проекту** в реальному часі
- 🚨 **Виявляє** застарілі, невикористовувані та дублюючі файли
- 📊 **Генерує** звіти про здоров'я архітектури
- 🤖 **Інтегрується** з Cascade для розумних рекомендацій
- 📈 **Відстежує** залежності та циклічні посилання
- 📁 **Портативна** — локалізована в `codemap-system/`, можна копіювати на інші проекти

---

## 🚀 Швидкий старт (2 хвилини)

### 1. Встановити залежності
```bash
cd /Users/dev/Documents/GitHub/atlas4/codemap-system
pip install -r requirements.txt
```

### 2. Запустити синхронізацію
```bash
bash /Users/dev/Documents/GitHub/atlas4/.windsurf/workflows/sync-architecture-on-startup.md
```

### 3. Перезавантажити Windsurf
- **Mac**: Cmd+Q → Відкрити знову
- **Linux/Windows**: Закрити → Відкрити знову

### 4. Використовувати в Cascade
```
@cascade get_architecture_overview()
```

---

## 📁 Що було створено

### Нові файли

| Файл                                                  | Призначення                     |
| ----------------------------------------------------- | ------------------------------- |
| `codemap-system/.env.architecture`                    | Конфігурація системи            |
| `codemap-system/mcp_architecture_server.py`           | MCP сервер для архітектури      |
| `codemap-system/requirements.txt`                     | Залежності (оновлено)           |
| `.windsurf/workflows/sync-architecture-on-startup.md` | Workflow синхронізації          |
| `ARCHITECTURE_INTEGRATION_GUIDE.md`                   | Повний гайд інтеграції          |
| `.env.example`                                        | Приклад конфігурації (оновлено) |

### Оновлені файли

- `codemap-system/requirements.txt` — додано 20+ залежностей
- `.env.example` — додано 50+ параметрів архітектури

---

## 🛠️ Компоненти системи

### 1. **Architecture Mapper** (`architecture_mapper.py`)
Аналізує архітектуру проекту:
- Рекурсивний аналіз файлів
- Побудова графу залежностей
- Виявлення циклічних посилань
- Оцінка здоров'я архітектури

### 2. **MCP Architecture Server** (`mcp_architecture_server.py`) — НОВИЙ
Інтегрує архітектурну систему з Cascade:
- 12 інструментів для аналізу
- JSON-RPC протокол
- Реальний час комунікація
- Експортування звітів

### 3. **MCP Architecture Tools** (`mcp_architecture_tools.py`)
Допоміжні інструменти:
- Отримання метрик архітектури
- Аналіз файлів та шарів
- Генерування рекомендацій

### 4. **Configuration** (`.env.architecture`)
Налаштування системи:
- Параметри аналізу
- Правила виявлення
- Інтеграція з Windsurf

---

## 📊 Доступні інструменти

### Основні команди

```bash
# Огляд архітектури
@cascade get_architecture_overview()

# Статус файлу
@cascade analyze_file_status(file_path: "orchestrator/app.js")

# Застарілі файли
@cascade detect_deprecated_files(days_threshold: 90)

# Невикористовувані файли
@cascade detect_unused_files()

# Циклічні залежності
@cascade detect_circular_dependencies()

# Граф залежностей
@cascade get_dependency_graph(file_path: "orchestrator/app.js", depth: 2)

# Здоров'я архітектури
@cascade get_architecture_health()

# Рекомендації рефакторингу
@cascade get_refactoring_recommendations(priority: "high")

# Аналіз шару
@cascade analyze_layer(layer_name: "business")

# Вплив змін
@cascade get_file_impact(file_path: "orchestrator/app.js")

# Експортування звітів
@cascade export_architecture_report(format: "json")

# Виявлення дублікатів
@cascade detect_duplicates(directory: "orchestrator")
```

---

## 🔧 Конфігурація

### Основні параметри

```bash
# Глибина аналізу залежностей
MAX_ANALYSIS_DEPTH=5

# Інтервал автоаналізу (сек)
AUTO_ANALYSIS_INTERVAL=300

# Поріг для застарілих файлів (дні)
DEPRECATED_THRESHOLD_DAYS=90

# Мінімальна оцінка здоров'я (0-100)
MIN_HEALTH_SCORE=60

# Максимальна складність функції
MAX_CYCLOMATIC_COMPLEXITY=10

# Виключити з аналізу
EXCLUDE_PATTERNS=node_modules,__pycache__,.git,.venv,dist,build

# Розширення для аналізу
ANALYZE_EXTENSIONS=.js,.ts,.jsx,.tsx,.py,.java,.cpp,.go

# Шари архітектури
ARCHITECTURE_LAYERS=presentation,business,data,infrastructure,utilities
```

Повна конфігурація в `codemap-system/.env.architecture`

---

## 📈 Приклади використання

### Сценарій 1: Перевірка здоров'я

```
Ви: Перевір здоров'я архітектури

@cascade get_architecture_health()

Результат:
✅ Архітектура в хорошому стані (72/100)
⚠️ Є 3 невикористовувані файли
⚠️ Є 1 циклічна залежність
```

### Сценарій 2: Знайти файли для видалення

```
Ви: Які файли можна видалити?

@cascade detect_unused_files()

Результат:
- orchestrator/legacy/old_api.js (2KB, 45 рядків)
- orchestrator/legacy/deprecated_utils.js (1KB, 20 рядків)
```

### Сценарій 3: План рефакторингу

```
Ви: Дай план рефакторингу з високим пріоритетом

@cascade get_refactoring_recommendations(priority: "high")

Результат:
1. Видалити невикористовувані файли (3 файли)
2. Розірвати циклічні залежності (1 цикл)
3. Консолідувати дублікати коду (5 місць)
```

---

## 🔄 Автоматизація

### При запуску Windsurf

Система автоматично:
1. Завантажує конфігурацію
2. Аналізує архітектуру
3. Запускає MCP сервер
4. Генерує звіти

### Періодичний аналіз

Кожні 5 хвилин (налаштовується):
- Перевіряє зміни в файлах
- Оновлює граф залежностей
- Генерує нові звіти

### Інтеграція з CI/CD

```bash
# Перевірка перед комітом
python3 << 'PYTHON'
import sys
sys.path.insert(0, 'codemap-system')
from mcp_architecture_server import ArchitectureAnalysisServer

server = ArchitectureAnalysisServer()
health = server._get_architecture_health()

if health['score'] < 60:
    print("❌ Архітектура потребує покращення")
    sys.exit(1)
PYTHON
```

---

## 📊 Звіти

### Формати звітів

- **JSON** — для програмної обробки
- **HTML** — для перегляду в браузері
- **Markdown** — для документації

### Розташування

```bash
codemap-system/reports/
├── architecture_report.json
├── architecture_report.html
└── architecture_report.markdown
```

### Переглянути звіти

```bash
# JSON
cat codemap-system/reports/architecture_report.json | jq

# HTML
open codemap-system/reports/architecture_report.html

# Markdown
cat codemap-system/reports/architecture_report.markdown
```

---

## 🔍 Моніторинг

### Логи

```bash
# Основні логи
tail -f codemap-system/logs/architecture_server.log

# Логи аналізатора
tail -f codemap-system/logs/analyzer.log

# Останні 100 рядків
tail -100 codemap-system/logs/architecture_server.log
```

### Статус системи

```bash
# Чи запущено сервер?
ps aux | grep mcp_architecture_server

# Чи є звіти?
ls -lh codemap-system/reports/

# Чи є логи?
ls -lh codemap-system/logs/
```

---

## 🐛 Налагодження

### Проблема: Сервер не запускається

```bash
# 1. Перевірте конфігурацію
cat codemap-system/.env.architecture

# 2. Перевірте залежності
pip install -r codemap-system/requirements.txt

# 3. Протестуйте вручну
python3 codemap-system/mcp_architecture_server.py

# 4. Перевірте логи
tail -50 codemap-system/logs/architecture_server.log
```

### Проблема: Архітектура не аналізується

```bash
# 1. Запустіть аналіз вручну
python3 << 'PYTHON'
import sys
sys.path.insert(0, 'codemap-system')
from architecture_mapper import ArchitectureMapper
from pathlib import Path

mapper = ArchitectureMapper(Path('.'))
result = mapper.analyze_architecture()
print(result)
PYTHON

# 2. Перевірте логи
tail -100 codemap-system/logs/architecture_server.log
```

### Проблема: Повільний аналіз

```bash
# 1. Зменшіть глибину аналізу
MAX_ANALYSIS_DEPTH=3

# 2. Збільшіть інтервал
AUTO_ANALYSIS_INTERVAL=600

# 3. Виключіть великі папки
EXCLUDE_PATTERNS=node_modules,__pycache__,.git,.venv,dist,build,archive
```

---

## 📚 Документація

- **[ARCHITECTURE_INTEGRATION_GUIDE.md](ARCHITECTURE_INTEGRATION_GUIDE.md)** — Повний гайд інтеграції
- **[codemap-system/.env.architecture](codemap-system/.env.architecture)** — Конфігурація
- **[.windsurf/workflows/sync-architecture-on-startup.md](.windsurf/workflows/sync-architecture-on-startup.md)** — Workflow синхронізації

---

## ✅ Чек-лист інтеграції

- [x] Встановлені залежності
- [x] Конфігурація створена
- [x] MCP сервер реалізований
- [x] Workflow синхронізації створено
- [x] Документація написана
- [ ] Залежності встановлені (`pip install -r codemap-system/requirements.txt`)
- [ ] Синхронізація запущена
- [ ] Windsurf перезавантажено
- [ ] Тестовані команди в Cascade

---

## 🎯 Наступні кроки

### 1. Встановити залежності
```bash
cd /Users/dev/Documents/GitHub/atlas4/codemap-system
pip install -r requirements.txt
```

### 2. Запустити синхронізацію
```bash
bash /Users/dev/Documents/GitHub/atlas4/.windsurf/workflows/sync-architecture-on-startup.md
```

### 3. Перезавантажити Windsurf
Cmd+Q → Відкрити знову

### 4. Протестувати команди
```
@cascade get_architecture_overview()
@cascade detect_unused_files()
@cascade get_architecture_health()
```

### 5. Налаштувати за потребою
Редагуйте `codemap-system/.env.architecture`

---

## 📞 Допомога

Якщо щось не працює:

1. **Перевірте конфігурацію**:
   ```bash
   cat codemap-system/.env.architecture
   ```

2. **Перевірте залежності**:
   ```bash
   pip list | grep -E "radon|networkx|pydantic"
   ```

3. **Перевірте логи**:
   ```bash
   tail -100 codemap-system/logs/architecture_server.log
   ```

4. **Перезавантажте Windsurf**

5. **Спробуйте ще раз**

---

## 🎉 Готово!

Архітектурна система повністю інтегрована з Windsurf.

**Тепер Cascade завжди має актуальну інформацію про архітектуру проекту!**

---

**Версія**: 1.0.0  
**Статус**: ✅ ГОТОВО  
**Дата**: 22 листопада 2025
