# 🔧 MCP Setup Guide - Як Cascade керує інструментами

## 📚 Механізм роботи

### 1. **Архітектура**

```
┌─────────────────────────────────────────────────────────────┐
│ Windsurf IDE                                                │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Cascade (AI Assistant)                                  │ │
│ │ - Аналізує ваші завдання                                │ │
│ │ - Вибирає потрібні інструменти                          │ │
│ │ - Формує запити                                         │ │
│ └──────────────────┬──────────────────────────────────────┘ │
│                    │                                         │
│                    ↓                                         │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ mcp_config.json (конфігурація)                          │ │
│ │ {                                                       │ │
│ │   "mcpServers": {                                       │ │
│ │     "codemap": {                                        │ │
│ │       "command": "python3",                             │ │
│ │       "args": ["mcp_server.py"]                         │ │
│ │     }                                                   │ │
│ │   }                                                     │ │
│ │ }                                                       │ │
│ └──────────────────┬──────────────────────────────────────┘ │
│                    │                                         │
│                    ↓                                         │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ MCP Protocol Handler (JSON-RPC через stdio)             │ │
│ │ - Запускає mcp_server.py як subprocess                  │ │
│ │ - Надсилає JSON запити в stdin                          │ │
│ │ - Читає JSON відповіді з stdout                         │ │
│ └──────────────────┬──────────────────────────────────────┘ │
└────────────────────┼──────────────────────────────────────────┘
                     │
                     ↓
        ┌────────────────────────────┐
        │ mcp_server.py              │
        │ (MCP Server Process)       │
        │                            │
        │ - analyze_file_deeply()    │
        │ - find_duplicates()        │
        │ - analyze_dependencies()   │
        │ - find_dead_code()         │
        │ - generate_refactoring()   │
        │ - get_project_summary()    │
        │                            │
        └────────────────────────────┘
```

### 2. **Процес взаємодії**

#### Крок 1: Ви ставите завдання
```
Ви: "Проаналізуй файл orchestrator/app.js"
```

#### Крок 2: Cascade обирає інструмент
```
Cascade: "Мені потрібен analyze_file_deeply"
```

#### Крок 3: Windsurf дивиться в mcp_config.json
```json
{
  "codemap": {
    "command": "python3",
    "args": ["mcp_server.py"]
  }
}
```

#### Крок 4: Windsurf запускає сервер
```bash
python3 /path/to/mcp_server.py
```

#### Крок 5: Windsurf надсилає JSON запит
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "analyze_file_deeply",
    "arguments": {
      "file_path": "orchestrator/app.js"
    }
  }
}
```

#### Крок 6: Сервер обробляє запит
```python
def analyze_file_deeply(self, file_path: str) -> str:
    # Читає файл
    # Аналізує структуру
    # Повертає результат
```

#### Крок 7: Сервер повертає JSON відповідь
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "📊 Аналіз файлу...\n{...}"
      }
    ]
  }
}
```

#### Крок 8: Cascade отримує результат
```
Cascade: "Ось результат аналізу..."
```

---

## 🛠️ Налаштування

### Конфігурація Windsurf

**Локальна конфігурація** (для цього проекту):
```
~/.codeium/windsurf/mcp_config.json
```

**Глобальна конфігурація** (для всіх проектів):
```
~/.codeium/windsurf/mcp_config.json
```

### Поточна конфігурація

```json
{
  "mcpServers": {
    "codemap": {
      "command": "python3",
      "args": [
        "/Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_server.py"
      ],
      "env": {
        "PYTHONPATH": "/Users/dev/Documents/GitHub/atlas4/codemap-system",
        "PROJECT_ROOT": "/Users/dev/Documents/GitHub/atlas4",
        "PYTHONUNBUFFERED": "1"
      }
    }
  }
}
```

---

## 📋 Доступні інструменти

### 1. **analyze_file_deeply**
Глибокий аналіз файлу: структура, функції, залежності, якість коду

```
@cascade analyze_file_deeply(file_path: "orchestrator/app.js")
```

### 2. **find_duplicates**
Знайти дублікати коду в директорії

```
@cascade find_duplicates(directory: "orchestrator/workflow")
```

### 3. **analyze_dependencies**
Аналіз залежностей файлу

```
@cascade analyze_dependencies(file_path: "orchestrator/app.js")
```

### 4. **find_dead_code**
Знайти мертвий код (невикористовувані функції, змінні)

```
@cascade find_dead_code(directory: "orchestrator")
```

### 5. **generate_refactoring_plan**
Генерувати план рефакторингу на основі аналізу

```
@cascade generate_refactoring_plan(priority: "high")
```

### 6. **get_project_summary**
Отримати загальну інформацію про проект

```
@cascade get_project_summary()
```

---

## 🔍 Тестування

### Тест 1: Ініціалізація сервера
```bash
echo '{"jsonrpc": "2.0", "id": 1, "method": "initialize", "params": {}}' | \
  python3 /Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_server.py
```

### Тест 2: Список інструментів
```bash
echo '{"jsonrpc": "2.0", "id": 2, "method": "tools/list", "params": {}}' | \
  python3 /Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_server.py
```

### Тест 3: Виклик інструменту
```bash
echo '{"jsonrpc": "2.0", "id": 3, "method": "tools/call", "params": {"name": "get_project_summary", "arguments": {}}}' | \
  python3 /Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_server.py
```

---

## 🚀 Як це працює без явного запуску сервера

### Ключовий момент: **Windsurf запускає сервер автоматично**

1. Коли ви використовуєте інструмент в Cascade
2. Windsurf читає `mcp_config.json`
3. Знаходить команду: `"command": "python3"`
4. Знаходить аргументи: `"args": ["mcp_server.py"]`
5. **Запускає процес**: `python3 mcp_server.py`
6. Надсилає запит через stdin
7. Читає відповідь з stdout
8. Завершує процес (або тримає його живим для наступних запитів)

### Переваги цього підходу

✅ **Простота**: Не потрібно вручну запускати сервер  
✅ **Безпека**: Код виконується локально  
✅ **Швидкість**: Немає затримок мережі  
✅ **Масштабованість**: Можна додавати нові інструменти  
✅ **Контроль**: Повна контроль над кодом та даними  

---

## 📝 Додавання нових інструментів

Щоб додати новий інструмент:

1. **Додайте в `_initialize_tools()`**:
```python
{
    "name": "my_new_tool",
    "description": "Опис інструменту",
    "inputSchema": {
        "type": "object",
        "properties": {
            "param1": {"type": "string", "description": "..."}
        },
        "required": ["param1"]
    }
}
```

2. **Додайте обробник в `handle_call_tool()`**:
```python
elif tool_name == "my_new_tool":
    result = self.my_new_tool(tool_args.get("param1"))
```

3. **Реалізуйте метод**:
```python
def my_new_tool(self, param1: str) -> str:
    # Ваша логіка
    return "Результат"
```

---

## 🎯 Висновок

Система MCP дозволяє Cascade (AI-асистенту) керувати потужними інструментами аналізу коду **без необхідності явного запуску сервера**. Windsurf автоматично:

1. Читає конфігурацію з `mcp_config.json`
2. Запускає MCP сервер як subprocess
3. Комунікує через JSON-RPC протокол
4. Повертає результати Cascade

Це створює **безшовну інтеграцію** між AI-асистентом та локальними інструментами аналізу.
