# 🌐 Глобальна інтеграція з Windsurf

## Огляд

Система архітектури тепер встановлює MCP сервер на **двох рівнях**:

1. **Локальний рівень** - у папці проекту (`.windsurf/mcp_config.json`)
2. **Глобальний рівень** - у Windsurf конфігурації (`~/.codeium/windsurf/mcp_config.json`)

## Що змінилося

### INSTALL.sh

Скрипт інсталяції тепер:

✅ Встановлює MCP конфіг у локальну папку проекту  
✅ Встановлює MCP конфіг у глобальну Windsurf папку  
✅ Перевіряє, чи конфіг вже існує перед перезаписом  
✅ Додає сервер до існуючого конфіга, якщо він вже є  

### Файли конфігурації

**Локальний конфіг:**
```
/Users/dev/Documents/GitHub/atlas4/.windsurf/mcp_config.json
```

**Глобальний конфіг:**
```
/Users/dev/.codeium/windsurf/mcp_config.json
```

## Як це працює

### При запуску INSTALL.sh:

1. **Локальна інтеграція:**
   - Створює `.windsurf/mcp_config.json` у корені проекту
   - Налаштовує MCP сервер для цього проекту

2. **Глобальна інтеграція:**
   - Перевіряє наявність `~/.codeium/windsurf/mcp_config.json`
   - Якщо конфіг порожній або не існує - створює новий
   - Якщо конфіг існує - додає сервер до нього
   - Якщо сервер вже є - пропускає (не перезаписує)

## Переваги

✅ **Глобальна доступність** - MCP сервер доступний у всіх проектах  
✅ **Локальна гнучкість** - кожен проект може мати свої налаштування  
✅ **Безпечність** - не перезаписує існуючі конфіги  
✅ **Автоматизація** - все налаштовується при інсталяції  

## Перевірка інтеграції

Щоб перевірити, чи система правильно розгортається:

```bash
# Перевірити локальний конфіг
cat .windsurf/mcp_config.json

# Перевірити глобальний конфіг
cat ~/.codeium/windsurf/mcp_config.json

# Тестувати MCP сервер
python3 codemap-system/windsurf/mcp_architecture_server.py
```

## Структура конфіга

```json
{
  "mcpServers": {
    "codemap": {
      "command": "python3",
      "args": [
        "/Users/dev/Documents/GitHub/atlas4/codemap-system/windsurf/mcp_architecture_server.py"
      ],
      "env": {
        "PYTHONPATH": "/Users/dev/Documents/GitHub/atlas4/codemap-system",
        "PROJECT_ROOT": "/Users/dev/Documents/GitHub/atlas4",
        "PYTHONUNBUFFERED": "1"
      }
    }
  }
}
```

## Налаштування Windsurf

Додатково налаштовуються Cascade параметри у `.windsurf/settings.json`:

```json
{
  "windsurf.cascade.context.includes": [
    "dependency-graph",
    "dead-code-analysis",
    "project-structure"
  ],
  "windsurf.cascade.maxContextTokens": 128000,
  "windsurf.cascade.autoRefresh": true,
  "windsurf.cascade.refreshInterval": 30000
}
```

## Розв'язування проблем

### Глобальний конфіг не створюється

Переконайтесь, що папка `~/.codeium/windsurf/` існує:

```bash
mkdir -p ~/.codeium/windsurf
```

### MCP сервер не запускається

Перевірте, що всі залежності встановлені:

```bash
cd codemap-system
source venv/bin/activate
pip install -r requirements-minimal.txt
```

### Шляхи не правильні

Переконайтесь, що INSTALL.sh запущений з папки `codemap-system`:

```bash
cd codemap-system
bash INSTALL.sh
```

## Подальші кроки

1. Перезавантажте Windsurf IDE
2. Перевірте, чи MCP сервер з'являється у списку доступних сервісів
3. Використовуйте команди архітектурного аналізу через Cascade
