# 🔗 Інтеграція MCP з Windsurf/Cascade - Повна Інструкція

**Дата**: 2025-11-19 02:09 UTC+02:00  
**Версія**: 1.0  
**Статус**: ✅ ЗАТВЕРДЖЕНО  

---

## 📋 Огляд

Це комплексна інструкція з правильної інтеграції Model Context Protocol (MCP) з Windsurf/Cascade для безпечного рефакторингу та чистки коду.

---

## 🎯 Архітектура MCP + Windsurf

```
┌─────────────────────────────────────────────────────────┐
│                    Windsurf IDE                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │              Cascade (LLM Client)                 │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  MCP Client (Native Integration)            │  │  │
│  │  │  - Manages MCP connections                  │  │  │
│  │  │  - Routes tool requests                     │  │  │
│  │  │  - Handles responses                        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
│                        ↓                                 │
│  ~/.codeium/windsurf/mcp_config.json                    │
│  (MCP Server Configuration)                             │
└─────────────────────────────────────────────────────────┘
         ↓              ↓              ↓
    ┌────────┐    ┌────────┐    ┌────────┐
    │ MCP    │    │ MCP    │    │ MCP    │
    │Server 1│    │Server 2│    │Server N│
    │(stdio) │    │(stdio) │    │(http)  │
    └────────┘    └────────┘    └────────┘
         ↓              ↓              ↓
    ┌────────┐    ┌────────┐    ┌────────┐
    │ Tools  │    │ Tools  │    │ Tools  │
    │ 1-20   │    │ 21-40  │    │ 41-100 │
    └────────┘    └────────┘    └────────┘
```

---

## 📁 Структура Конфігурації

### 1. **Основний конфіг Windsurf**

```json
~/.codeium/windsurf/mcp_config.json
{
  "mcpServers": {
    "codemap": {
      "command": "python3",
      "args": [
        "/Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_codemap_server.py",
        "--project",
        "/Users/dev/Documents/GitHub/atlas4",
        "--mode",
        "stdio"
      ],
      "env": {
        "PYTHONPATH": "/Users/dev/Documents/GitHub/atlas4/codemap-system",
        "PYTHONUNBUFFERED": "1"
      }
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "<YOUR_TOKEN>"
      }
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/Users/dev/Documents/GitHub/atlas4"]
    }
  }
}
```

### 2. **Локальний конфіг проєкту** (для версійного контролю)

```
orchestrator/.windsurf/mcp_config.json
(Копія основного конфігу для синхронізації)
```

### 3. **Конфіг Cascade**

```
orchestrator/.windsurf/settings.json
(Налаштування Cascade для проєкту)
```

---

## 🔧 Типи MCP Серверів

### Тип 1: STDIO (Standard Input/Output)

**Для локальних серверів:**

```json
{
  "mcpServers": {
    "codemap": {
      "command": "python3",
      "args": [
        "/path/to/mcp_codemap_server.py",
        "--project", ".",
        "--mode", "stdio"
      ],
      "env": {
        "PYTHONPATH": "/path/to/codemap-system",
        "PYTHONUNBUFFERED": "1"
      }
    }
  }
}
```

**Переваги:**
- ✅ Прямий доступ до локальних ресурсів
- ✅ Немає мережевих затримок
- ✅ Легко дебагити

**Недоліки:**
- ❌ Процес запускається для кожного запиту
- ❌ Більше системних ресурсів

### Тип 2: HTTP (HyperText Transfer Protocol)

**Для віддалених серверів:**

```json
{
  "mcpServers": {
    "figma": {
      "serverUrl": "https://your-server.com/mcp"
    }
  }
}
```

**Переваги:**
- ✅ Постійне з'єднання
- ✅ Менше системних ресурсів
- ✅ Легко масштабувати

**Недоліки:**
- ❌ Мережеві затримки
- ❌ Залежність від мережі

---

## 🔐 Безпека та Синхронізація

### Крок 1: Налаштування Дозволів

```bash
# Встановити дозволи на конфіг
chmod 600 ~/.codeium/windsurf/mcp_config.json

# Перевірити дозволи
ls -la ~/.codeium/windsurf/mcp_config.json
# Результат: -rw------- (600)
```

### Крок 2: Синхронізація Конфігів

```bash
# Копіювати з основного конфігу в проєкт
cp ~/.codeium/windsurf/mcp_config.json \
   orchestrator/.windsurf/mcp_config.json

# Перевірити синхронізацію
diff ~/.codeium/windsurf/mcp_config.json \
     orchestrator/.windsurf/mcp_config.json
```

### Крок 3: Версійний Контроль

```bash
# Додати до .gitignore (НЕ комітити токени!)
echo "~/.codeium/windsurf/mcp_config.json" >> .gitignore

# Комітити тільки структуру (без токенів)
git add orchestrator/.windsurf/mcp_config.template.json
git commit -m "chore: add MCP config template"
```

### Крок 4: Перевірка Конфігу

```bash
# Валідувати JSON
python3 -m json.tool ~/.codeium/windsurf/mcp_config.json

# Перевірити синтаксис
jq . ~/.codeium/windsurf/mcp_config.json
```

---

## 🚀 Інтеграція Codemap MCP

### Крок 1: Запуск Codemap MCP Server

```bash
# Перейти в папку codemap-system
cd /Users/dev/Documents/GitHub/atlas4/codemap-system

# Запустити MCP сервер
python3 mcp_codemap_server.py --project . --mode stdio
```

### Крок 2: Додати до mcp_config.json

```json
{
  "mcpServers": {
    "codemap": {
      "command": "python3",
      "args": [
        "/Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_codemap_server.py",
        "--project",
        "/Users/dev/Documents/GitHub/atlas4",
        "--mode",
        "stdio"
      ],
      "env": {
        "PYTHONPATH": "/Users/dev/Documents/GitHub/atlas4/codemap-system",
        "PYTHONUNBUFFERED": "1"
      }
    }
  }
}
```

### Крок 3: Перезавантажити Windsurf

```bash
# Закрити та відкрити Windsurf
# Або натиснути: Cmd+Shift+P → "Reload Window"
```

### Крок 4: Перевірити Підключення

```bash
# У Cascade chat:
# Запитати: "Show me available MCP tools"
# Результат: Має показати codemap tools
```

---

## 📊 Доступні Інструменти Codemap

### Ресурси Codemap MCP

```
codemap://analysis/summary
  └─ Останній аналіз проєкту

codemap://analysis/dead-code
  └─ Виявлений мертвий код

codemap://analysis/cycles
  └─ Циклічні залежності

codemap://analysis/full
  └─ Повні дані аналізу
```

### Використання в Cascade

```
@Cascade: Analyze the dead code in orchestrator using Codemap

Cascade буде:
1. Запитати Codemap MCP сервер
2. Отримати дані про мертвий код
3. Проаналізувати результати
4. Дати рекомендації
```

---

## 🔄 Синхронізація під час Рефакторингу

### Крок 1: Перед Рефакторингом

```bash
# 1. Оновити аналіз
cd codemap-system
python3 codemap_analyzer.py --once

# 2. Синхронізувати конфіги
cp ~/.codeium/windsurf/mcp_config.json \
   orchestrator/.windsurf/mcp_config.json

# 3. Перевірити MCP сервер
curl http://localhost:8000/health 2>/dev/null || echo "MCP server not running"
```

### Крок 2: Під час Рефакторингу

```bash
# Використовувати Cascade з MCP:
# 1. Запитати Codemap про залежності
# 2. Перевірити, чи код використовується
# 3. Видалити тільки справді мертвий код
# 4. Запустити тести
```

### Крок 3: Після Рефакторингу

```bash
# 1. Запустити тести
npm test

# 2. Оновити аналіз
cd codemap-system
python3 codemap_analyzer.py --once

# 3. Перевірити результати
cat reports/CODEMAP_SUMMARY.md

# 4. Синхронізувати конфіги
cp ~/.codeium/windsurf/mcp_config.json \
   orchestrator/.windsurf/mcp_config.json

# 5. Коміт
git add -A
git commit -m "refactor: clean up dead code with MCP analysis"
```

---

## ⚙️ Налаштування Інструментів

### Крок 1: Відкрити Plugin Manager

```
Windsurf → Plugins → Manage Plugins
```

### Крок 2: Налаштувати Codemap

```
Codemap Plugin
├─ Status: Enabled
├─ Tools: 
│  ├─ ✅ analyze_project
│  ├─ ✅ find_dead_code
│  ├─ ✅ detect_cycles
│  ├─ ✅ get_analysis_summary
│  └─ ✅ compare_versions
└─ Refresh: Click to reload
```

### Крок 3: Перевірити Ліміти

```
Cascade Tool Limit: 100 tools max
Current Usage:
  - Codemap: 5 tools
  - GitHub: 20 tools
  - Filesystem: 10 tools
  - Total: 35/100 tools
```

---

## 🔍 Дебагування

### Проблема: MCP сервер не запускається

```bash
# 1. Перевірити конфіг
python3 -m json.tool ~/.codeium/windsurf/mcp_config.json

# 2. Запустити вручну
python3 /Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_codemap_server.py \
  --project /Users/dev/Documents/GitHub/atlas4 \
  --mode stdio

# 3. Перевірити помилки
# Результат має показати: "MCP server started"
```

### Проблема: Інструменти не видно в Cascade

```bash
# 1. Перезавантажити Windsurf
# Cmd+Shift+P → "Reload Window"

# 2. Перевірити логи
# Windsurf → Help → Toggle Developer Tools

# 3. Перевірити конфіг
grep -A 10 "codemap" ~/.codeium/windsurf/mcp_config.json
```

### Проблема: Повільна робота

```bash
# 1. Перевірити кількість інструментів
# Має бути < 100 всього

# 2. Вимкнути непотрібні інструменти
# Windsurf → Plugins → Manage Plugins → Disable unused

# 3. Перевірити ресурси
# Activity Monitor → Memory/CPU usage
```

---

## ✅ Чек-лист Інтеграції

### Встановлення
- [ ] Windsurf встановлено
- [ ] Cascade активовано
- [ ] MCP підтримується (Enterprise або включено в settings)

### Конфігурація
- [ ] `~/.codeium/windsurf/mcp_config.json` створено
- [ ] Codemap MCP сервер налаштовано
- [ ] Дозволи встановлені (600)
- [ ] JSON валідний

### Синхронізація
- [ ] Конфіг синхронізовано з проєктом
- [ ] .gitignore налаштовано
- [ ] Токени не комічені

### Тестування
- [ ] MCP сервер запускається
- [ ] Інструменти видно в Cascade
- [ ] Запити до MCP працюють
- [ ] Результати коректні

### Рефакторинг
- [ ] Аналіз проведено
- [ ] Залежності перевірені
- [ ] Код видалено акуратно
- [ ] Тести проходять
- [ ] Конфіги синхронізовані

---

## 🎯 Рекомендації для Безпечного Рефакторингу

### 1. Завжди використовуйте MCP для аналізу

```
Перед видаленням коду:
1. Запитати Codemap про залежності
2. Перевірити, чи код використовується
3. Перевірити в інших файлах
4. Тільки потім видалити
```

### 2. Синхронізуйте конфіги регулярно

```bash
# Після кожного значного рефакторингу
cp ~/.codeium/windsurf/mcp_config.json \
   orchestrator/.windsurf/mcp_config.json
```

### 3. Запускайте тести після змін

```bash
# Завжди перевіряйте функціональність
npm test
npm start
```

### 4. Документуйте причину видалення

```javascript
// REMOVED 2025-11-19: Unused variable from legacy parser
// Verified with Codemap MCP: no dependencies found
// const unusedVar = 'test';
```

---

## 📚 Посилання

### Офіційна Документація
- [MCP Specification](https://modelcontextprotocol.io/)
- [Windsurf MCP Integration](https://docs.codeium.com/windsurf/mcp)
- [MCP Server Repository](https://github.com/modelcontextprotocol/servers)

### Наші Інструменти
- Codemap MCP: `/Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_codemap_server.py`
- Конфіг: `~/.codeium/windsurf/mcp_config.json`
- Проєкт: `/Users/dev/Documents/GitHub/atlas4/orchestrator`

---

## 🎉 Висновок

**Правильна інтеграція MCP з Windsurf/Cascade:**

1. ✅ Налаштувати `mcp_config.json` з правильними серверами
2. ✅ Синхронізувати конфіги між системою та проєктом
3. ✅ Використовувати MCP для аналізу перед рефакторингом
4. ✅ Запускати тести після змін
5. ✅ Документувати причину видалення

**Результат:**
- 🟢 Безпечний рефакторинг
- 🟢 Повна синхронізація
- 🟢 Мінімум помилок
- 🟢 Максимум контролю

---

**Статус**: ✅ ГОТОВО ДО ВИКОРИСТАННЯ  
**Автор**: Cascade AI + MCP Codemap  
**Дата**: 2025-11-19 02:09 UTC+02:00
