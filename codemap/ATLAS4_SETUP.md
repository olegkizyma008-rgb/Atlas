# 🚀 Atlas4 Codemap Setup

Система аналізу кодових графів для проєкту Atlas4.

## ✅ Статус

**Система розгорнута і працює! ✅**

```
✅ 164 файли проаналізовано
✅ 253 функції знайдено
✅ 322 вузли у графі залежностей
✅ 0 циклічних залежностей
✅ Постійне спостереження активне
✅ Windsurf готовий до роботи
```

## 📊 Аналіз Atlas4

### Граф залежностей
- **Вузли**: 322
- **Ребра**: 447
- **Циклічні залежності**: 0 ✅

### Метрики
- **Середня кількість імпортів на файл**: 3.34
- **Середня кількість функцій на файл**: 3.67
- **Максимальна глибина залежностей**: 0

### Мертвий код
- **Невикористовувані функції**: 469
- **Невикористовувані приватні методи**: 0

## 📁 Папки для аналізу

```
atlas4/
├── orchestrator/       # Основна логіка
├── services/           # Сервіси
├── scripts/            # Скрипти
├── mcp-servers/        # MCP сервери
└── config/             # Конфігурація
```

## 📊 Звіти

Звіти створюються в папці `codemap/reports/`:

```
reports/
├── CODEMAP_SUMMARY.md       # Для Windsurf Cascade
├── codemap_analysis.json    # Повні дані
└── codemap_analysis.html    # HTML звіт
```

## 🪟 Windsurf Integration

### Workflows готові до використання

```
Ctrl+L → /update-codemap
Ctrl+L → /analyze-dead-code
Ctrl+L → /detect-cycles
Ctrl+L → /refactor-with-context
```

### Як використовувати

1. **Відкрий Windsurf Cascade:**
   ```
   Ctrl+L
   ```

2. **Запусти workflow:**
   ```
   /update-codemap
   ```

3. **Cascade матиме:**
   - ✅ Граф залежностей
   - ✅ Мертвий код
   - ✅ Циклічні залежності
   - ✅ Метрики складності

## 🔄 Постійне спостереження

Система автоматично:
- 👁️ Спостерігає за змінами кожні 5 секунд
- 📊 Оновлює звіти при кожній зміні
- 💾 Зберігає попередні версії в `reports/.backup/`
- 🔗 Надає Windsurf актуальні дані

```bash
👁️  Watching for changes...
✅ Analysis complete at 20:48:32
✅ Analysis complete at 20:48:37
✅ Analysis complete at 20:48:42
```

## 🛡️ Захист даних

### Резервне копіювання
- Попередні звіти зберігаються в `reports/.backup/`
- Кожна версія має timestamp: `YYYYMMDD_HHMMSS`
- Можна повернутися до старої версії

### Версіонування workflows
- Версія workflows: `1.0.0`
- Зберігається в `.windsurf/workflows/VERSION`
- Автоматично оновлюється при розгортанні

## 📋 Конфігурація

**Файл:** `config.yaml`

```yaml
project:
  name: "Atlas4 Project"
  root: "../"

analysis:
  include_paths:
    - "orchestrator"
    - "services"
    - "scripts"
    - "mcp-servers"
    - "config"
  
  exclude_paths:
    - "node_modules"
    - "__pycache__"
    - ".git"
    - "legacy-archive"
    - "backups"
    - "logs"
```

## 🚀 Як запустити

### Перший запуск (вже зроблено)
```bash
cd /Users/dev/Documents/GitHub/atlas4/codemap
./deploy.sh
```

### Повторний запуск
```bash
./deploy.sh
```

Система:
1. Перевіряє встановлення
2. Оновлює workflows (якщо потрібно)
3. Запускає аналіз
4. Запускає постійне спостереження

### Зупинення
```bash
Ctrl+C
```

## 📊 Переглядання звітів

### Markdown звіт (для Cascade)
```bash
cat reports/CODEMAP_SUMMARY.md
```

### JSON дані
```bash
cat reports/codemap_analysis.json | jq
```

### HTML звіт
```bash
open reports/codemap_analysis.html
```

## 🎯 Сценарії використання

### Сценарій 1: Аналіз нового модуля

1. Додай нову папку в `include_paths` в `config.yaml`
2. Запусти: `./deploy.sh`
3. Система автоматично проаналізує новий модуль
4. Windsurf матиме оновлену інформацію

### Сценарій 2: Пошук мертвого коду

1. Відкрий Windsurf Cascade: `Ctrl+L`
2. Запусти: `/analyze-dead-code`
3. Cascade покаже всі невикористовувані функції
4. Видали мертвий код

### Сценарій 3: Виявлення циклічних залежностей

1. Відкрий Windsurf Cascade: `Ctrl+L`
2. Запусти: `/detect-cycles`
3. Cascade покаже всі циклічні залежності
4. Рефакторити, щоб розбити цикли

### Сценарій 4: Рефакторинг з контекстом

1. Відкрий Windsurf Cascade: `Ctrl+L`
2. Запусти: `/refactor-with-context`
3. Cascade матиме:
   - Граф залежностей
   - Мертвий код
   - Циклічні залежності
4. Cascade запропонує оптимальний рефакторинг

## 📞 Команди

### Запуск аналізу один раз
```bash
python3 codemap_analyzer.py --once
```

### Запуск постійного спостереження
```bash
python3 codemap_analyzer.py --watch
```

### Запуск з конкретним конфігом
```bash
python3 codemap_analyzer.py --config /path/to/config.yaml --once
```

## 🎉 Результат

✅ **Система повністю розгорнута для atlas4**

- ✅ 164 файли проаналізовано
- ✅ Граф залежностей побудовано
- ✅ Мертвий код виявлено
- ✅ Windsurf готовий до роботи
- ✅ Постійне спостереження активне
- ✅ Дані захищені резервним копіюванням

## 🚀 Windsurf готовий!

```
Ctrl+L → /update-codemap
```

Cascade матиме 100% контекст atlas4 проєкту! 🎉

---

**Система готова до роботи!**
