# Architecture System v2.0 - Advanced Requirements
# Для максимальної потужності та інформативності аналізу

# === БАЗОВІ (з requirements-minimal.txt) ===
radon>=6.0.0
networkx>=3.0
python-dotenv>=1.0.0
watchdog>=3.0.0
pathspec>=0.12.0
pydantic>=2.0.0
pyyaml>=6.0
click>=8.0.0
colorama>=0.4.6
tqdm>=4.60.0
websockets>=12.0
pytest>=7.0.0
pytest-cov>=4.0.0
jsonschema>=4.0.0
mcp>=1.0.0

# === АСИНХРОННІСТЬ (ПОТУЖНА) ===
aiofiles>=23.0.0              # Асинхронна робота з файлами
asyncio-contextmanager>=1.0.0 # Контекст менеджери для asyncio

# === АНАЛІЗ КОДУ (ГЛИБОКИЙ) ===
pylint>=2.17.0                # Статичний аналіз
flake8>=6.0.0                 # Перевірка стилю
bandit>=1.7.0                 # Аналіз безпеки
semgrep>=1.40.0               # Пошук паттернів

# === ГРАФ ЗАЛЕЖНОСТЕЙ (ВІЗУАЛІЗАЦІЯ) ===
graphviz>=0.20.0              # Graphviz Python
plotly>=5.0.0                 # Інтерактивні графіки

# === КЕШУВАННЯ (ШВИДКІСТЬ) ===
diskcache>=5.6.0              # Дисковий кеш (Redis не потрібен для локалу)
cachetools>=5.3.0             # Локальне кешування

# === МОНІТОРИНГ ПРОДУКТИВНОСТІ ===
psutil>=5.9.0                 # Моніторинг CPU, RAM, I/O
memory-profiler>=0.61.0       # Профайлинг пам'яті

# === ОБРОБКА ДАНИХ ===
polars>=0.19.0                # Швидша обробка даних
numpy>=1.24.0                 # Числові обчислення

# === РОЗПОДІЛЕНІ ОБЧИСЛЕННЯ (ПОТУЖНІ) ===
ray>=2.0.0                    # Розподілені обчислення (ОБОВ'ЯЗКОВО для Рівня 4)
dask>=2023.0.0                # Паралельні обчислення

# === ДОКУМЕНТАЦІЯ ===
sphinx>=6.0.0                 # Генерація документації
sphinx-rtd-theme>=1.2.0       # Тема для Sphinx
