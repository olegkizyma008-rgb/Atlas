# ⚡ Quick Start - Architecture System v2.0

**Запустіть систему за 5 хвилин!**

---

## 🚀 Швидкий старт (5 хвилин)

### 1️⃣ Встановлення (2 хвилини)

```bash
# Перейдіть в папку
cd codemap-system

# Запустіть автоматичну інсталяцію
bash INSTALL.sh
```

Це автоматично:
- ✅ Встановить залежності
- ✅ Створить віртуальне середовище
- ✅ Налаштує локальний MCP конфіг (`.windsurf/mcp_config.json`)
- ✅ Налаштує глобальний MCP конфіг (`~/.codeium/windsurf/mcp_config.json`)
- ✅ Налаштує Windsurf Cascade параметри

### 2️⃣ Запуск (1 хвилина)

```bash
# Дайте права на виконання
chmod +x START_FULL_SYSTEM.sh

# Запустіть систему
./START_FULL_SYSTEM.sh
```

### 3️⃣ Використання (2 хвилини)

Система автоматично запустить:
- ✅ MCP Architecture Server (порт 8766)
- ✅ WebSocket Server (порт 8765)
- ✅ File Monitor
- ✅ Architecture Daemon

---

## 📊 Що вона робить?

### 🔍 Аналіз архітектури
```bash
# Аналізує залежності між файлами
# Виявляє циклічні залежності
# Знаходить невикористовувані файли
# Оцінює якість коду
```

### 🔒 Аналіз безпеки
```bash
# Виявляє SQL injection
# Знаходить hardcoded secrets
# Перевіряє на insecure deserialization
# Виявляє слабку криптографію
```

### ⚡ Аналіз продуктивності
```bash
# Виявляє N+1 query problems
# Знаходить неефективні цикли
# Перевіряє на глибоку вкладеність
# Виявляє витоки пам'яті
```

### 🔧 Рекомендації рефакторингу
```bash
# Пропонує розбити циклічні залежності
# Рекомендує видалити невикористовувані файли
# Пропонує зменшити зв'язність модулів
# Рекомендує покращити назви
```

---

## 💻 Команди для Windsurf IDE

Після запуску системи можете використовувати команди:

```
/architecture analyze              # Аналіз архітектури
/architecture dependencies <file>  # Залежності файлу
/architecture unused               # Невикористовувані файли
/architecture circular             # Циклічні залежності
/architecture duplicates           # Дублікати коду
/architecture refactor             # Рекомендації
/architecture health               # Здоров'я архітектури
/architecture report               # Експорт звіту
/architecture security             # Аналіз безпеки
/architecture performance          # Аналіз продуктивності
```

---

## 🔌 WebSocket для real-time оновлень

Підключіться до WebSocket для отримання оновлень в реальному часі:

```javascript
const ws = new WebSocket('ws://localhost:8765');

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('Architecture update:', data);
};
```

---

## 📁 Структура папок

```
codemap-system/
├── core/                    # Ядро аналізу
├── analysis/                # Розширені аналізатори
├── windsurf/                # Інтеграція з Windsurf
├── logs/                    # Логи (автоматично створюється)
├── reports/                 # Звіти (автоматично створюється)
├── .cache/                  # Кеш (автоматично створюється)
├── .env.architecture        # Конфігурація
├── requirements.txt         # Залежності
├── START_FULL_SYSTEM.sh     # Запуск системи
├── STOP_FULL_SYSTEM.sh      # Зупинення системи
└── DEPLOYMENT_GUIDE.md      # Повний гайд
```

---

## 🛑 Зупинення системи

```bash
# Зупиніть систему
./STOP_FULL_SYSTEM.sh

# Або вручну
pkill -f architecture_daemon
pkill -f mcp_architecture_server
pkill -f websocket_server
```

---

## 📊 Перевірка статусу

```bash
# Переглядайте логи
tail -f logs/architecture.log

# Перевіряйте процеси
ps aux | grep architecture

# Перевіряйте порти
lsof -i :8765
lsof -i :8766
```

---

## 🧪 Тестування

```bash
# Запустіть тести
python3 quick_test.py

# Очікуваний результат:
# ✅ test_architecture_analysis - PASSED
# ✅ test_circular_dependency_detection - PASSED
# ✅ test_duplication_detection - PASSED
# ✅ test_quality_analysis - PASSED
# ✅ test_security_analysis - PASSED
# ✅ test_performance_analysis - PASSED
# 6/6 tests passed ✅
```

---

## 🔧 Конфігурація

Редагуйте `.env.architecture`:

```bash
# Шлях до проекту
PROJECT_ROOT=..

# Глибина аналізу
ANALYSIS_DEPTH=2

# Інтервал аналізу (секунди)
ANALYSIS_INTERVAL=300

# Розширення файлів
FILE_EXTENSIONS=.py,.js,.ts,.jsx,.tsx

# Папки для ігнорування
IGNORE_PATTERNS=node_modules,__pycache__,.git,.venv,dist,build

# Логування
LOG_LEVEL=INFO

# WebSocket
WEBSOCKET_HOST=localhost
WEBSOCKET_PORT=8765

# MCP
MCP_PORT=8766
```

---

## 📈 Статистика

**Система включає:**
- 17 файлів
- 4500+ рядків коду
- 10 команд Cascade
- 9 інструментів MCP
- 5 аналізаторів
- 8 типів сповіщень

---

## 🌐 Глобальна інтеграція з Windsurf

Система автоматично налаштовується на **двох рівнях**:

1. **Локальний рівень** - для цього проекту
   - `.windsurf/mcp_config.json` - MCP конфіг
   - `.windsurf/settings.json` - Windsurf параметри

2. **Глобальний рівень** - для всіх проектів
   - `~/.codeium/windsurf/mcp_config.json` - Глобальний MCP конфіг

Це означає, що MCP сервер буде доступний у всіх проектах Windsurf!

Для деталей див. **GLOBAL_INTEGRATION.md**

---

## 📚 Документація

- **DEPLOYMENT_GUIDE.md** - Повний гайд розгортання
- **GLOBAL_INTEGRATION.md** - Глобальна інтеграція з Windsurf
- **REFACTORING_PLAN.md** - План реалізації
- **RUN_DAEMON.md** - Запуск daemon
- **QUICK_START.md** - Цей файл

---

## ❓ Часті питання

**Q: Як змінити порти?**
A: Редагуйте `.env.architecture` та встановіть `WEBSOCKET_PORT` та `MCP_PORT`

**Q: Як переглядати логи?**
A: `tail -f logs/architecture.log`

**Q: Як зупинити систему?**
A: `./STOP_FULL_SYSTEM.sh`

**Q: Як перезапустити систему?**
A: `./STOP_FULL_SYSTEM.sh && ./START_FULL_SYSTEM.sh`

**Q: Як запустити окремий компонент?**
A: `python3 windsurf/mcp_architecture_server.py`

---

## 🎯 Наступні кроки

1. ✅ Запустіть систему: `./START_FULL_SYSTEM.sh`
2. ✅ Перевірте логи: `tail -f logs/architecture.log`
3. ✅ Інтегруйте з Windsurf (див. DEPLOYMENT_GUIDE.md)
4. ✅ Використовуйте команди в IDE
5. ✅ Моніторьте архітектуру в реальному часі

---

**Версія**: 2.0  
**Статус**: ✅ ГОТОВО  
**Дата**: 22 листопада 2025

**Питання?** Див. DEPLOYMENT_GUIDE.md або RUN_DAEMON.md
