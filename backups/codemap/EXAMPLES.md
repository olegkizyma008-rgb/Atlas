# 📚 Приклади використання

Практичні приклади того, як використовувати Codemap Analyzer.

## 🎯 Приклад 1: Знайти мертвий код

### Сценарій
Ти хочеш знайти всі невикористовувані функції в проєкті.

### Кроки

1. **Запусти аналіз:**
   ```bash
   python3 codemap_analyzer.py --once
   ```

2. **Переглянь звіт:**
   ```bash
   cat reports/CODEMAP_SUMMARY.md
   ```

3. **Результат:**
   ```markdown
   ## 🔴 Dead Code Detected

   ### Unused Functions (12)
   - `old_helper()` in `src/utils.py` (line 45)
   - `deprecated_api()` in `src/api.py` (line 123)
   - `unused_service()` in `src/services.py` (line 89)
   ...
   ```

4. **Видалення:**
   Відкрий файл і видали функцію:
   ```python
   # src/utils.py
   # ❌ Видалити
   def old_helper():
       return "not used"
   ```

### Використання з Cascade

```
Ctrl+L → /analyze-dead-code
```

Cascade покаже всі невикористовувані функції і запропонує видалити.

---

## 🔄 Приклад 2: Виявити циклічні залежності

### Сценарій
Ти хочеш знайти циклічні залежності, які ускладнюють архітектуру.

### Кроки

1. **Запусти аналіз:**
   ```bash
   python3 codemap_analyzer.py --once
   ```

2. **Переглянь циклі:**
   ```bash
   cat reports/CODEMAP_SUMMARY.md | grep -A 10 "Circular"
   ```

3. **Результат:**
   ```markdown
   ## 🔄 Circular Dependencies (3)
   1. auth_service → user_service → auth_service
   2. models → serializers → models
   3. api → utils → api
   ```

4. **Рішення:**
   Винести спільний код в окремий модуль:
   ```
   auth_service → common_utils
   user_service → common_utils
   ```

### Використання з Cascade

```
Ctrl+L → /detect-cycles
```

Cascade запропонує як розбити цикли.

---

## 🚀 Приклад 3: Рефакторинг з контекстом

### Сценарій
Ти хочеш рефакторити модуль, знаючи всю архітектуру.

### Кроки

1. **Запусти комплексний аналіз:**
   ```
   Ctrl+L → /refactor-with-context
   ```

2. **Cascade виконає:**
   - Запустить `/update-codemap` — граф залежностей
   - Запустить `/analyze-dead-code` — мертвий код
   - Запустить `/detect-cycles` — циклічні залежності

3. **Cascade матиме контекст:**
   - Знає, які модулі залежать від цього
   - Знає, який код мертвий
   - Знає про циклічні залежності

4. **Запитай Cascade:**
   ```
   Рефакторити цей модуль, щоб розбити циклічні залежності
   ```

5. **Cascade запропонує:**
   ```python
   # Перед
   # auth_service.py
   from user_service import get_user
   
   # user_service.py
   from auth_service import check_auth
   
   # Після
   # common.py
   def check_auth(user_id):
       ...
   
   def get_user(user_id):
       ...
   
   # auth_service.py
   from common import check_auth
   
   # user_service.py
   from common import get_user
   ```

---

## 📊 Приклад 4: Аналіз архітектури

### Сценарій
Новий розробник приходить в проєкт. Потрібно пояснити архітектуру.

### Кроки

1. **Запусти аналіз:**
   ```bash
   python3 codemap_analyzer.py --once
   ```

2. **Запитай Cascade:**
   ```
   Ctrl+L
   Поясни архітектуру проєкту за 5 речень на основі графу залежностей
   @reports/CODEMAP_SUMMARY.md
   ```

3. **Cascade відповість:**
   ```
   Проєкт складається з 3 основних шарів:
   
   1. API Layer (api.py, routes.py) — обробляє HTTP запити
   2. Business Logic (services.py, models.py) — основна логіка
   3. Data Layer (database.py, repositories.py) — робота з БД
   
   Залежності йдуть від API до Business Logic до Data Layer.
   Немає циклічних залежностей, архітектура чиста.
   ```

---

## 🔍 Приклад 5: Постійне спостереження під час розробки

### Сценарій
Ти розробляєш нову функцію і хочеш бачити, як змінюється граф залежностей.

### Кроки

1. **Запусти аналізатор у режимі спостереження:**
   ```bash
   python3 codemap_analyzer.py --watch
   ```

   Вивід:
   ```
   👁️  Watching for changes...
   ✅ Analysis complete at 20:30:45
   ✅ Analysis complete at 20:30:50
   ✅ Analysis complete at 20:30:55
   ```

2. **Редагуй код в іншому терміналі:**
   ```bash
   # Термінал 2
   vim src/new_feature.py
   ```

3. **Аналізатор автоматично оновлює звіти:**
   ```bash
   # Звіти оновлюються кожні 5 секунд
   cat reports/CODEMAP_SUMMARY.md
   ```

4. **Відкрий Cascade і запитай:**
   ```
   Ctrl+L
   Покажи, як змінилась архітектура після моїх змін
   @reports/CODEMAP_SUMMARY.md
   ```

---

## 🎯 Приклад 6: Pre-commit Hook

### Сценарій
Ти хочеш, щоб звіти автоматично оновлювались при кожному коміті.

### Кроки

1. **Встанови pre-commit:**
   ```bash
   pip install pre-commit
   pre-commit install
   ```

2. **Редагуй код:**
   ```bash
   vim src/utils.py
   ```

3. **Зроби коміт:**
   ```bash
   git add .
   git commit -m "Add new utility function"
   ```

4. **Pre-commit автоматично:**
   - Запустить `codemap_analyzer.py --once`
   - Оновить звіти
   - Дозволить коміт

5. **Звіти оновлені:**
   ```bash
   git log --oneline
   # Коміт містить оновлені звіти
   ```

---

## 🌐 Приклад 7: GitHub Actions

### Сценарій
Ти хочеш, щоб звіти оновлювались при кожному push на GitHub.

### Кроки

1. **Створи файл `.github/workflows/codemap.yml`:**
   ```yaml
   name: Update Codemap

   on:
     push:
       branches: [main, develop]

   jobs:
     analyze:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v3
         - uses: actions/setup-python@v4
           with:
             python-version: '3.11'
         - run: pip install -r requirements.txt
         - run: python3 codemap_analyzer.py --once
         - uses: actions/upload-artifact@v3
           with:
             name: codemap-reports
             path: reports/
   ```

2. **Push на GitHub:**
   ```bash
   git push origin main
   ```

3. **GitHub Actions автоматично:**
   - Запустить аналізатор
   - Завантажить звіти як artifact

4. **Переглянь результати:**
   - Відкрий GitHub Actions
   - Завантаж artifact `codemap-reports`

---

## 💡 Приклад 8: Кастомний workflow

### Сценарій
Ти хочеш створити власний workflow для спеціального аналізу.

### Кроки

1. **Створи файл `.windsurf/workflows/my-analysis.md`:**
   ```markdown
   # My Custom Analysis

   Спеціальний аналіз для мого проєкту

   ## Кроки

   1. Оновлюємо карту
      Call /update-codemap

   2. Аналізуємо мертвий код
      Call /analyze-dead-code

   3. Знаходимо складні модулі
      Які модулі мають найбільше залежностей?
      @reports/codemap_analysis.json

   4. Пропонуємо рефакторинг
      На основі графу залежностей, запропонуй як розбити найскладніші модулі
   ```

2. **Використовуй workflow:**
   ```
   Ctrl+L → /my-analysis
   ```

3. **Cascade виконає всі кроки:**
   - Оновить карту
   - Аналізує мертвий код
   - Знайде складні модулі
   - Запропонує рефакторинг

---

## 🔧 Приклад 9: Налаштування для Python проєкту

### Сценарій
Ти маєш Python проєкт і хочеш налаштувати аналізатор.

### Кроки

1. **Налаштуй `config.yaml`:**
   ```yaml
   project:
     name: "My Python Project"
     root: "./"

   analysis:
     include_paths:
       - "src"
       - "app"
       - "lib"
     exclude_paths:
       - "tests"
       - "venv"
       - "__pycache__"
       - ".pytest_cache"
     file_extensions:
       - ".py"
   ```

2. **Запусти аналіз:**
   ```bash
   python3 codemap_analyzer.py --once
   ```

3. **Переглянь результати:**
   ```bash
   cat reports/CODEMAP_SUMMARY.md
   ```

---

## 🌐 Приклад 10: Налаштування для JavaScript проєкту

### Сценарій
Ти маєш JavaScript проєкт і хочеш налаштувати аналізатор.

### Кроки

1. **Налаштуй `config.yaml`:**
   ```yaml
   project:
     name: "My JavaScript Project"
     root: "./"

   analysis:
     include_paths:
       - "src"
       - "lib"
       - "components"
     exclude_paths:
       - "node_modules"
       - "dist"
       - "build"
       - "test"
     file_extensions:
       - ".js"
       - ".jsx"
       - ".ts"
       - ".tsx"
   ```

2. **Запусти аналіз:**
   ```bash
   python3 codemap_analyzer.py --once
   ```

3. **Переглянь результати:**
   ```bash
   cat reports/CODEMAP_SUMMARY.md
   ```

---

## 📈 Приклад 11: Аналіз метрик

### Сценарій
Ти хочеш отримати метрики складності проєкту.

### Кроки

1. **Запусти аналіз:**
   ```bash
   python3 codemap_analyzer.py --once
   ```

2. **Переглянь метрики:**
   ```bash
   cat reports/codemap_analysis.json | jq '.complexity_metrics'
   ```

3. **Результат:**
   ```json
   {
     "average_imports_per_file": 3.5,
     "average_functions_per_file": 4.2,
     "max_dependency_depth": 5
   }
   ```

4. **Інтерпретація:**
   - **average_imports_per_file: 3.5** — кожен файл в середньому імпортує 3.5 модулі
   - **average_functions_per_file: 4.2** — кожен файл має в середньому 4.2 функції
   - **max_dependency_depth: 5** — максимальна глибина залежностей 5 рівнів

---

## 🎓 Приклад 12: Інтеграція з IDE

### Сценарій
Ти хочеш запускати аналіз прямо з VS Code.

### Кроки

1. **Створи `.vscode/tasks.json`:**
   ```json
   {
     "version": "2.0.0",
     "tasks": [
       {
         "label": "Update Codemap",
         "type": "shell",
         "command": "python3 codemap_analyzer.py --once",
         "group": {
           "kind": "build",
           "isDefault": true
         }
       },
       {
         "label": "Watch Codemap",
         "type": "shell",
         "command": "python3 codemap_analyzer.py --watch",
         "isBackground": true
       }
     ]
   }
   ```

2. **Запусти з VS Code:**
   - Ctrl+Shift+B → "Update Codemap"
   - Ctrl+Shift+B → "Watch Codemap"

3. **Результат:**
   - Аналіз запускається прямо з IDE
   - Звіти оновлюються автоматично

---

## ✅ Чек-лист прикладів

- [ ] Приклад 1: Знайти мертвий код
- [ ] Приклад 2: Виявити циклічні залежності
- [ ] Приклад 3: Рефакторинг з контекстом
- [ ] Приклад 4: Аналіз архітектури
- [ ] Приклад 5: Постійне спостереження
- [ ] Приклад 6: Pre-commit Hook
- [ ] Приклад 7: GitHub Actions
- [ ] Приклад 8: Кастомний workflow
- [ ] Приклад 9: Python проєкт
- [ ] Приклад 10: JavaScript проєкт
- [ ] Приклад 11: Аналіз метрик
- [ ] Приклад 12: Інтеграція з IDE

---

**Готово! Тепер ти знаєш, як використовувати Codemap Analyzer! 🚀**
