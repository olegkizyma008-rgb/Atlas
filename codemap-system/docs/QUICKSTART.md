# ⚡ Швидкий старт Codemap Analyzer

## 1️⃣ Встановлення (30 секунд)

```bash
# Встанови залежності
pip install -r requirements.txt

# Перевір, що все працює
python3 codemap_analyzer.py --once
```

## 2️⃣ Перегляд результатів

Після запуску ти отримаєш звіти в папці `reports/`:

```bash
# Прочитай звіт для Cascade
cat reports/CODEMAP_SUMMARY.md

# Або переглянь HTML у браузері
open reports/codemap_analysis.html
```

## 3️⃣ Використання з Windsurf Cascade

### Способ 1: Workflows (найпростіше)

Відкрий Cascade в Windsurf:
```
Ctrl+L
```

Напиши одну з команд:
```
/update-codemap
/analyze-dead-code
/detect-cycles
/refactor-with-context
```

### Способ 2: Ручне запитання

```
Ctrl+L → Покажи граф залежностей для цього файлу
@reports/CODEMAP_SUMMARY.md
```

Cascade прочитає звіт і поясниме залежності.

## 4️⃣ Постійне оновлення

Запусти аналізатор у режимі спостереження:

```bash
python3 codemap_analyzer.py --watch
```

Аналізатор буде оновлювати звіти кожні 5 секунд.

## 5️⃣ Налаштування для твого проєкту

Відредагуй `config.yaml`:

```yaml
project:
  name: "My Project"
  root: "./"  # ← Твоя папка проєкту

analysis:
  include_paths:
    - "src"      # ← Папки для аналізу
    - "lib"
  exclude_paths:
    - "node_modules"  # ← Папки для ігнорування
    - "__pycache__"
```

## 6️⃣ Автоматизація при коміті

```bash
# Встанови pre-commit
pip install pre-commit
pre-commit install

# Тепер при кожному git commit звіти автоматично оновляться
git commit -m "My changes"
# → Автоматично запустить codemap_analyzer.py
```

## 📊 Що ти отримаєш

### CODEMAP_SUMMARY.md
Звіт для Cascade з:
- Графом залежностей
- Мертвим кодом
- Циклічними залежностями
- Метриками складності

### codemap_analysis.json
Повні дані для програмної обробки

### codemap_analysis.html
Красивий HTML звіт для браузера

## 🎯 Приклади запитів до Cascade

Після запуску `/update-codemap` ти можеш запитати Cascade:

```
Покажи всі циклічні залежності в проєкті
```

```
Які функції ніхто не викликає? Запропонуй видалити
```

```
Поясни архітектуру проєкту на основі графу залежностей
```

```
Які модулі найбільш залежні від інших?
```

```
Рефакторити цей код, щоб розбити циклічні залежності
```

## ✅ Готово!

Твоя система аналізу коду для Windsurf працює! 🚀

Запусти:
```bash
python3 codemap_analyzer.py --once
```

Потім відкрий Cascade:
```
Ctrl+L → /update-codemap
```

Cascade матиме 100% контекст твого проєкту! 🎉
