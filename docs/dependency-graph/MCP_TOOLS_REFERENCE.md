# 🔧 Довідка MCP Інструментів

**Дата:** 23 листопада 2025  
**Версія:** 2.0  
**Статус:** ✅ Готово

---

## 📚 Доступні MCP Інструменти

Всі ці інструменти доступні через Windsurf IDE та можуть використовуватися для аналізу архітектури.

---

## 🎯 Основні Інструменти

### 1. `mcp0_get_architecture_overview()`
**Опис:** Отримати огляд архітектури проекту

**Параметри:** Немає

**Повертає:**
- Загальну статистику файлів
- Розподіл за статусом (ACTIVE, UNUSED, IN_DEVELOPMENT)
- Оцінку здоров'я архітектури
- Кількість циклічних залежностей

**Приклад:**
```python
result = mcp0_get_architecture_overview()
print(result['statistics'])  # Статистика
print(result['health_score'])  # Оцінка здоров'я
```

**Коли використовувати:** На початку аналізу для розуміння загальної ситуації

---

### 2. `mcp0_get_architecture_health()`
**Опис:** Отримати оцінку здоров'я архітектури та рекомендації

**Параметри:** Немає

**Повертає:**
- Оцінку здоров'я (0-100)
- Коефіцієнт невикористовуваних файлів
- Середню кількість залежностей
- Рекомендації для покращення

**Приклад:**
```python
result = mcp0_get_architecture_health()
print(f"Оцінка: {result['health']['score']}/100")
print(f"Рекомендації: {result['recommendations']}")
```

**Коли використовувати:** Для оцінки якості архітектури

---

### 3. `mcp0_detect_circular_dependencies()`
**Опис:** Виявити циклічні залежності в архітектурі

**Параметри:** Немає

**Повертає:**
- Список циклічних залежностей
- Кількість циклів
- Файли, які беруть участь у циклах

**Приклад:**
```python
result = mcp0_detect_circular_dependencies()
if result['count'] > 0:
    print(f"Знайдено {result['count']} циклів!")
    for cycle in result['circular_dependencies']:
        print(f"Цикл: {' → '.join(cycle)}")
else:
    print("Циклічні залежності не знайдені ✅")
```

**Коли використовувати:** Для перевірки якості архітектури

---

### 4. `mcp0_detect_unused_files()`
**Опис:** Виявити невикористовувані файли

**Параметри:** Немає

**Повертає:**
- Список невикористовуваних файлів
- Розмір кожного файлу
- Кількість рядків коду

**Приклад:**
```python
result = mcp0_detect_unused_files()
print(f"Знайдено {result['count']} невикористовуваних файлів")
for file in result['unused_files'][:10]:  # Перші 10
    print(f"- {file['file']} ({file['lines']} рядків)")
```

**Коли використовувати:** Для пошуку файлів для видалення

---

### 5. `mcp0_detect_duplicates(min_lines=5)`
**Опис:** Виявити дублікати коду

**Параметри:**
- `min_lines` (int, default=5): Мінімальна довжина блоку для вважання дублікатом

**Повертає:**
- Список дублікатів
- Хеш дублікату
- Кількість повторень

**Приклад:**
```python
result = mcp0_detect_duplicates(min_lines=10)
print(f"Знайдено {result['count']} дублікатів")
for dup in result['duplicates'][:5]:  # Перші 5
    print(f"Дублікат (хеш: {dup['hash']}) в {len(dup['files'])} файлах")
```

**Коли використовувати:** Для пошуку дублікатів коду

---

### 6. `mcp0_detect_broken_files()`
**Опис:** Виявити файли з синтаксичними помилками

**Параметри:** Немає

**Повертає:**
- Список файлів з помилками
- Причину помилки
- Розмір файлу

**Приклад:**
```python
result = mcp0_detect_broken_files()
if result['count'] > 0:
    print(f"⚠️ Знайдено {result['count']} файлів з помилками!")
    for file in result['broken_files']:
        print(f"- {file['file']}: {file['reason']}")
else:
    print("Файлів з помилками не знайдено ✅")
```

**Коли використовувати:** Для перевірки синтаксису коду

---

### 7. `mcp0_analyze_file_status(file_path)`
**Опис:** Аналізувати статус конкретного файлу

**Параметри:**
- `file_path` (str): Шлях до файлу

**Повертає:**
- Статус файлу (ACTIVE, UNUSED, IN_DEVELOPMENT)
- Розмір та кількість рядків
- Залежності файлу
- Кількість залежних файлів

**Приклад:**
```python
result = mcp0_analyze_file_status("orchestrator/ai/context-aware-tool-filter.js")
print(f"Статус: {result['status']}")
print(f"Залежностей: {len(result['dependencies'])}")
print(f"Залежних файлів: {result['dependents_count']}")
```

**Коли використовувати:** Для аналізу конкретного файлу

---

### 8. `mcp0_get_dependency_graph(file_path, depth=2)`
**Опис:** Отримати граф залежностей для файлу

**Параметри:**
- `file_path` (str): Шлях до файлу
- `depth` (int, default=2): Глибина графу

**Повертає:**
- Граф залежностей
- Вузли (файли)
- Ребра (залежності)

**Приклад:**
```python
result = mcp0_get_dependency_graph("services/whisper/whisper_service.py", depth=3)
print(f"Корінь: {result['root']}")
print(f"Вузлів: {len(result['nodes'])}")
print(f"Ребер: {len(result['edges'])}")
for edge in result['edges']:
    print(f"  {edge['from']} → {edge['to']}")
```

**Коли використовувати:** Для аналізу залежностей файлу

---

### 9. `mcp0_get_refactoring_recommendations(priority="medium")`
**Опис:** Отримати рекомендації для рефакторингу

**Параметри:**
- `priority` (str): "low", "medium", "high"

**Повертає:**
- Список рекомендацій
- Тип проблеми (unused, deprecated)
- Рекомендована дія

**Приклад:**
```python
result = mcp0_get_refactoring_recommendations(priority="high")
print(f"Рекомендацій (HIGH): {len(result['items'])}")
for item in result['items'][:10]:
    print(f"- {item['file']}: {item['action']}")
```

**Коли використовувати:** Для отримання рекомендацій рефакторингу

---

### 10. `mcp0_export_architecture_report(format="json")`
**Опис:** Експортувати детальний звіт архітектури

**Параметри:**
- `format` (str): "json", "html", "markdown"

**Повертає:**
- Шлях до файлу звіту
- Формат звіту
- Статус експорту

**Приклад:**
```python
result = mcp0_export_architecture_report(format="markdown")
print(f"Звіт експортований: {result['report_path']}")
print(f"Формат: {result['format']}")
```

**Коли використовувати:** Для експорту звіту в різні формати

---

## 🔄 Типові Сценарії Використання

### Сценарій 1: Швидкий Аналіз (5 хвилин)
```python
# 1. Отримати огляд
overview = mcp0_get_architecture_overview()
print(f"Файлів: {overview['summary']['total_files']}")
print(f"Активних: {overview['summary']['active_files']}")

# 2. Перевірити здоров'я
health = mcp0_get_architecture_health()
print(f"Оцінка: {health['health']['score']}/100")

# 3. Перевірити циклічні залежності
cycles = mcp0_detect_circular_dependencies()
print(f"Циклів: {cycles['count']}")
```

### Сценарій 2: Пошук Невикористовуваних Файлів (10 хвилин)
```python
# 1. Отримати список невикористовуваних
unused = mcp0_detect_unused_files()
print(f"Невикористовуваних: {unused['count']}")

# 2. Проаналізувати кожен файл
for file in unused['unused_files'][:5]:
    status = mcp0_analyze_file_status(file['file'])
    print(f"{file['file']}: {status['status']}")
    print(f"  Залежностей: {len(status['dependencies'])}")
```

### Сценарій 3: Аналіз Залежностей (15 хвилин)
```python
# 1. Отримати граф залежностей
graph = mcp0_get_dependency_graph("services/whisper/whisper_service.py", depth=3)
print(f"Залежностей: {len(graph['edges'])}")

# 2. Перевірити, чи є циклічні залежності
cycles = mcp0_detect_circular_dependencies()
if cycles['count'] > 0:
    print("⚠️ Знайдені циклічні залежності!")
```

### Сценарій 4: Пошук Дублікатів (10 хвилин)
```python
# 1. Знайти дублікати
duplicates = mcp0_detect_duplicates(min_lines=10)
print(f"Дублікатів: {duplicates['count']}")

# 2. Показати топ дублікатів
for dup in duplicates['duplicates'][:5]:
    print(f"Дублікат в {len(dup['files'])} файлах")
```

### Сценарій 5: Перевірка Синтаксису (5 хвилин)
```python
# 1. Знайти файли з помилками
broken = mcp0_detect_broken_files()
if broken['count'] > 0:
    print(f"⚠️ Файлів з помилками: {broken['count']}")
    for file in broken['broken_files']:
        print(f"- {file['file']}: {file['reason']}")
else:
    print("✅ Синтаксис в порядку")
```

---

## 💡 Поради та Трюки

### Совет 1: Використовуйте Фільтри
```python
# Отримайте тільки невикористовувані файли з певної папки
unused = mcp0_detect_unused_files()
orchestrator_unused = [f for f in unused['unused_files'] 
                       if 'orchestrator' in f['file']]
```

### Совет 2: Аналізуйте Залежності Перед Видаленням
```python
# Перед видаленням файлу, перевірте залежності
file_path = "orchestrator/ai/context-aware-tool-filter.js"
status = mcp0_analyze_file_status(file_path)
if len(status['dependencies']) == 0:
    print("✅ Безпечно видалити")
else:
    print(f"⚠️ Має {len(status['dependencies'])} залежностей")
```

### Совет 3: Експортуйте Звіти
```python
# Експортуйте звіт для документації
report = mcp0_export_architecture_report(format="markdown")
print(f"Звіт: {report['report_path']}")
```

### Совет 4: Моніторьте Прогрес
```python
# Запустіть аналіз до та після очистки
before = mcp0_get_architecture_overview()
# ... виконайте очистку ...
after = mcp0_get_architecture_overview()

print(f"Видалено файлів: {before['summary']['total_files'] - after['summary']['total_files']}")
print(f"Видалено рядків: {before['statistics']['total_lines'] - after['statistics']['total_lines']}")
```

---

## 🚀 Інтеграція з Windsurf

### Як Використовувати в Windsurf IDE

1. **Відкрийте Windsurf IDE**
2. **Натисніть Cmd+K** (або Ctrl+K на Windows/Linux)
3. **Введіть команду MCP:**
   ```
   mcp0_get_architecture_overview()
   ```
4. **Натисніть Enter**
5. **Отримайте результат**

### Приклади Команд для Windsurf

```
# Отримати огляд
mcp0_get_architecture_overview()

# Перевірити здоров'я
mcp0_get_architecture_health()

# Знайти циклічні залежності
mcp0_detect_circular_dependencies()

# Знайти невикористовувані файли
mcp0_detect_unused_files()

# Аналізувати файл
mcp0_analyze_file_status("path/to/file.js")

# Отримати граф залежностей
mcp0_get_dependency_graph("path/to/file.js", depth=3)

# Отримати рекомендації
mcp0_get_refactoring_recommendations(priority="high")

# Експортувати звіт
mcp0_export_architecture_report(format="markdown")
```

---

## 📊 Вихідні Формати

### JSON Формат
```json
{
  "timestamp": "2025-11-23T04:16:26.433086",
  "statistics": {
    "total_files": 620,
    "active_files": 300,
    "unused_files": 297
  },
  "health_score": {
    "score": 100,
    "modularity": "good"
  }
}
```

### Markdown Формат
```markdown
# 🏗️ Architecture Report

## 📊 Statistics
- **Total Files**: 620
- **Active Files**: 300
- **Unused Files**: 297
```

### HTML Формат
```html
<div class="architecture-report">
  <h1>Architecture Report</h1>
  <table>
    <tr><td>Total Files</td><td>620</td></tr>
    <tr><td>Active Files</td><td>300</td></tr>
  </table>
</div>
```

---

## 🔗 Посилання

- **ANALYSIS_SUMMARY.md** - Резюме аналізу
- **ARCHITECTURE_ANALYSIS_REPORT.md** - Детальний звіт
- **CLEANUP_RECOMMENDATIONS.md** - Рекомендації
- **QUICK_CLEANUP_COMMANDS.sh** - Автоматизація

---

**Довідка готова!** 📚

Дата: 23 листопада 2025, 04:16 UTC+02:00
