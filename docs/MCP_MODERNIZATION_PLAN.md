# План модернізації системи MCP інструментів
## На основі refactor.md та аналізу поточної системи
## Дата: 2025-10-29

## Фаза 1: Покращення схем та описів (Priority: HIGH)

### 1.1 Оновлення промптів з детальними описами
**Файли для модернізації:**
- `/prompts/mcp/tetyana_plan_tools_*.js` - всі спеціалізовані промпти

**Зміни:**
- Додати детальні описи для кожного інструменту (не просто "Create file", а повний опис з бізнес-логікою)
- Додати поле `when_to_use` для кожного інструменту
- Додати поле `common_mistakes` з типовими помилками

### 1.2 Впровадження enum для параметрів
**Файли:**
- `/orchestrator/ai/validation/schema-validator.js`
- Всі промпти в `/prompts/mcp/`

**Зміни:**
- Додати enum для всіх параметрів з обмеженим набором значень
- Приклад: status: enum["new", "processing", "completed", "failed"]

### 1.3 Few-shot examples в кожному промпті
**Файли:**
- Всі `/prompts/mcp/tetyana_plan_tools_*.js`

**Зміни:**
- Мінімум 3 приклади для кожного типу завдання
- Приклади мають покривати edge cases
- Додати приклади помилок та їх виправлення

## Фаза 2: Багаторівнева валідація (Priority: HIGH)

### 2.1 Цикл самокорекції
**Новий файл:**
- `/orchestrator/ai/validation/self-correction-validator.js`

**Інтеграція в:**
- `/orchestrator/ai/validation/validation-pipeline.js`

**Функціонал:**
```javascript
class SelfCorrectionValidator {
  async validate(toolPlan, context) {
    // 1. Відправити план назад до LLM
    // 2. Попросити перевірити на помилки
    // 3. Якщо є помилки - виправити
    // 4. Повторити до 3 разів
  }
}
```

### 2.2 Роутер-класифікатор
**Новий файл:**
- `/orchestrator/workflow/stages/router-classifier-processor.js`

**Модифікація:**
- `/orchestrator/workflow/executor-v3.js` - додати новий етап перед server-selection

**Функціонал:**
- Використовувати швидку модель (Gemini Flash) для початкової класифікації
- Визначати 1-2 релевантних сервери замість всіх 10+
- Передавати обмежений набір до основної моделі

### 2.3 Покращення MCP Sync Validator
**Файл:**
- `/orchestrator/ai/validation/mcp-sync-validator.js`

**Зміни:**
- Додати кешування схем інструментів (TTL: 5 хвилин)
- Покращити fuzzy matching з Levenshtein distance
- Додати auto-correction для типових помилок

## Фаза 3: Динамічні схеми та контекст (Priority: MEDIUM)

### 3.1 Контекстуальні інструменти
**Новий файл:**
- `/orchestrator/ai/context-aware-tool-filter.js`

**Інтеграція:**
- `/orchestrator/workflow/stages/tetyana-plan-tools-processor.js`

**Функціонал:**
```javascript
class ContextAwareToolFilter {
  filterTools(availableTools, context) {
    // Якщо немає файлів - приховати інструменти редагування
    // Якщо не запущено сервер - приховати playwright
    // Якщо немає git repo - приховати git інструменти
  }
}
```

### 3.2 Динамічна генерація схем
**Модифікація:**
- `/orchestrator/ai/tetyana-tool-system.js`

**Зміни:**
- Генерувати JSON Schema динамічно на основі контексту
- Додавати/видаляти параметри залежно від стану системи
- Адаптувати required fields під поточне завдання

## Фаза 4: Спеціалізовані промпти (Priority: HIGH)

### 4.1 Універсальний шаблон промпту
**Новий файл:**
- `/prompts/mcp/templates/universal-prompt-template.js`

**Структура згідно refactor.md:**
```javascript
const UNIVERSAL_TEMPLATE = `
You are Tetyana, a world-class AI agent and master MCP tool planner.
Your current mission is to create a precise, step-by-step tool plan.
You have been assigned: {{serverDomains}}.

CRITICAL DIRECTIVES:
1. SERVER & TOOL NAMES: ONLY use tools from AVAILABLE_TOOLS
2. PARAMETERS: ONLY use parameter names from inputSchema
3. NO INVENTIONS: DO NOT invent new tools or parameters
4. PRECISION: Rely on FEW-SHOT EXAMPLES as ground truth
5. EFFICIENCY: Create the most direct plan

SPECIALIZED PATTERNS:
{{specializedPatterns}}

VALIDATION CHECKLIST:
- [ ] Every tool name matches AVAILABLE_TOOLS
- [ ] Every parameter matches inputSchema
- [ ] All required values used correctly
- [ ] Plan is logical and addresses the action
`;
```

### 4.2 Оновлення всіх спеціалізованих промптів
**Файли:**
- Всі `/prompts/mcp/tetyana_plan_tools_*.js`

**Зміни:**
- Використовувати універсальний шаблон
- Додати SPECIALIZED PATTERNS для кожного сервера
- Додати VALIDATION CHECKLIST

## Фаза 5: Покращення надійності (Priority: HIGH)

### 5.1 State Machine для складних процесів
**Новий файл:**
- `/orchestrator/workflow/state-machine.js`

**Функціонал:**
```javascript
class WorkflowStateMachine {
  states = {
    INIT: 'init',
    PLANNING: 'planning',
    VALIDATING: 'validating',
    EXECUTING: 'executing',
    VERIFYING: 'verifying',
    COMPLETE: 'complete'
  }
  
  transitions = {
    init: ['planning'],
    planning: ['validating', 'error'],
    validating: ['executing', 'planning'],
    executing: ['verifying', 'error'],
    verifying: ['complete', 'executing']
  }
}
```

### 5.2 Circuit Breaker pattern
**Файл:**
- `/orchestrator/utils/circuit-breaker.js` (вже існує, потребує інтеграції)

**Інтеграція в:**
- `/orchestrator/ai/llm-client.js`
- `/orchestrator/ai/mcp-manager.js`

### 5.3 Exponential Backoff з jitter
**Файл:**
- `/orchestrator/utils/exponential-backoff.js` (вже існує, потребує покращення)

**Зміни:**
- Додати jitter для уникнення thundering herd
- Адаптивний backoff на основі типу помилки

## Фаза 6: Моніторинг та метрики (Priority: LOW)

### 6.1 Метрики виконання
**Новий файл:**
- `/orchestrator/monitoring/metrics-collector.js`

**Метрики:**
- Success rate по кожному серверу
- Середній час планування/виконання
- Частота помилок валідації
- Confidence distribution

### 6.2 Логування з структурованими даними
**Модифікація:**
- Всі processors в `/orchestrator/workflow/stages/`

**Зміни:**
- Structured logging з JSON
- Correlation ID для трейсингу
- Performance timing

## Порядок впровадження

### Тиждень 1 (HIGH Priority)
1. ✅ Фаза 1.1 - Детальні описи в промптах
2. ✅ Фаза 1.2 - Enum для параметрів  
3. ✅ Фаза 1.3 - Few-shot examples
4. ✅ Фаза 4.1-4.2 - Універсальний шаблон промпту

### Тиждень 2 (HIGH Priority)
5. ✅ Фаза 2.1 - Цикл самокорекції
6. ✅ Фаза 2.2 - Роутер-класифікатор
7. ✅ Фаза 2.3 - Покращення MCP Sync Validator
8. ✅ Фаза 5.1-5.3 - State Machine та надійність

### Тиждень 3 (MEDIUM Priority)
9. ✅ Фаза 3.1-3.2 - Динамічні схеми та контекст

### Тиждень 4 (LOW Priority)
10. ✅ Фаза 6.1-6.2 - Моніторинг та метрики

## Очікувані результати

### Покращення точності
- 📈 95%+ success rate для планування інструментів (зараз ~85%)
- 📈 99%+ валідність згенерованих JSON (зараз ~90%)
- 📈 90%+ правильний вибір серверів (зараз ~75%)

### Покращення швидкості
- ⚡ -30% час планування через роутер-класифікатор
- ⚡ -50% повторних спроб через самокорекцію
- ⚡ -20% загальний час виконання

### Покращення надійності
- 🛡️ 0% критичних збоїв через невалідні інструменти
- 🛡️ <1% timeout через exponential backoff
- 🛡️ 100% відновлення після тимчасових збоїв

## Критерії успіху
1. ✅ Всі промпти використовують універсальний шаблон
2. ✅ Validation pipeline має 5+ рівнів перевірки
3. ✅ Роутер-класифікатор зменшує набір серверів до 1-2
4. ✅ Self-correction виправляє 80%+ помилок автоматично
5. ✅ Метрики показують покращення на 20%+ по всіх KPI
