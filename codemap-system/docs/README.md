# 🗺️ Codemap Analyzer

Незалежна система для **постійного аналізу кодових графів**, виявлення мертвого коду та інтеграції з Windsurf Cascade.

## 🎯 Можливості

- ✅ **Граф залежностей** — повна карта імпортів та викликів
- ✅ **Виявлення мертвого коду** — невикористовувані функції, змінні, імпорти
- ✅ **Циклічні залежності** — знаходить та пропонує рішення
- ✅ **Метрики складності** — аналізує архітектуру проєкту
- ✅ **Постійне оновлення** — автоматично при кожній зміні
- ✅ **Інтеграція з Windsurf** — Cascade бачить всю інформацію
- ✅ **Багатомовна підтримка** — Python, JavaScript, TypeScript, Go, Java

## 🚀 Швидкий старт

### 1. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 2. Налаштування проєкту

Відредагуй `config.yaml`:

```yaml
project:
  name: "My Project"
  root: "./"

analysis:
  include_paths:
    - "src"
    - "lib"
    - "app"
  exclude_paths:
    - "node_modules"
    - "__pycache__"
    - ".git"
```

### 3. Запуск аналізу

**Один раз:**
```bash
python3 codemap_analyzer.py --once
```

**Постійне спостереження:**
```bash
python3 codemap_analyzer.py --watch
```

### 4. Переглянь звіти

- **Markdown** (для Cascade): `reports/CODEMAP_SUMMARY.md`
- **JSON** (для програм): `reports/codemap_analysis.json`
- **HTML** (для браузера): `reports/codemap_analysis.html`

## 📊 Структура звітів

### CODEMAP_SUMMARY.md

Цей файл автоматично читає Cascade і містить:

```markdown
# 📊 Code Analysis Report

## Project Overview
- Files Analyzed: 42
- Total Functions: 156
- Total Imports: 89

## Dependency Graph
- Nodes: 45
- Edges: 128
- Circular Dependencies: 3

## Dead Code Detected
### Unused Functions (12)
- `old_helper()` in `src/utils.py` (line 45)
- `deprecated_api()` in `src/api.py` (line 123)
...

## Circular Dependencies (3)
1. module_a → module_b → module_a
2. service_x → service_y → service_z → service_x
...
```

### codemap_analysis.json

Повні дані для програмної обробки:

```json
{
  "timestamp": "2025-11-18T20:30:00",
  "project": "My Project",
  "files_analyzed": 42,
  "total_functions": 156,
  "dependency_graph": {
    "nodes": 45,
    "edges": 128,
    "cycles": 3
  },
  "dead_code": {
    "functions": [...],
    "private_methods": [...],
    "imports": [...]
  },
  "file_imports": {...},
  "function_definitions": {...},
  "dependency_edges": [...]
}
```

## 🔧 Інтеграція з Windsurf

### Способ 1: Workflows (рекомендовано)

Workflows вже створені в `.windsurf/workflows/`:

1. **`/update-codemap`** — оновити всю карту
2. **`/analyze-dead-code`** — знайти мертвий код
3. **`/detect-cycles`** — виявити циклічні залежності
4. **`/refactor-with-context`** — рефакторити з повним контекстом

**Використання в Cascade:**

```
Ctrl+L → /update-codemap
Ctrl+L → /analyze-dead-code
Ctrl+L → /refactor-with-context
```

### Способ 2: Ручне викликання

Відкрий Cascade (Ctrl+L) і напиши:

```
Покажи граф залежностей для цього файлу
@reports/CODEMAP_SUMMARY.md
```

Cascade автоматично прочитає звіт і поясниме залежності.

### Способ 3: Pre-commit Hook (автоматично при кожному коміті)

```bash
pip install pre-commit
pre-commit install
```

Тепер при кожному `git commit` аналізатор автоматично оновить звіти.

## 📝 Конфігурація

### config.yaml

```yaml
project:
  name: "My Project"
  root: "./"

analysis:
  # Папки для аналізу
  include_paths:
    - "src"
    - "lib"
    - "app"
  
  # Папки для ігнорування
  exclude_paths:
    - "node_modules"
    - "__pycache__"
    - ".git"
    - "dist"
    - "build"
  
  # Розширення файлів
  file_extensions:
    - ".py"
    - ".js"
    - ".ts"
    - ".tsx"
    - ".jsx"
  
  # Мінімальний розмір функції
  min_function_size: 3

output:
  # Папка для звітів
  reports_dir: "reports"
  
  # Формати звітів
  formats:
    - "json"
    - "html"
    - "markdown"
  
  # Автоматичне оновлення
  auto_update: true
  
  # Інтервал перевірки (секунди)
  watch_interval: 5

dead_code_rules:
  unused_functions: true
  unused_variables: true
  unused_imports: true
  unused_private_methods: true

dependency_rules:
  detect_cycles: true
  max_depth: 5
  complexity_threshold: 10
```

## 🎓 Приклади використання

### Приклад 1: Знайти всі невикористовувані функції

```bash
python3 codemap_analyzer.py --once
cat reports/CODEMAP_SUMMARY.md | grep "Unused Functions" -A 20
```

### Приклад 2: Виявити циклічні залежності

```bash
python3 codemap_analyzer.py --once
cat reports/codemap_analysis.json | jq '.cycles'
```

### Приклад 3: Постійне спостереження під час розробки

```bash
# Термінал 1: Запусти аналізатор
python3 codemap_analyzer.py --watch

# Термінал 2: Редагуй код
# Аналізатор автоматично оновить звіти кожні 5 секунд
```

### Приклад 4: Рефакторинг з Cascade

1. Запусти Cascade: `Ctrl+L`
2. Напиши: `/refactor-with-context`
3. Cascade:
   - Запустить `/update-codemap`
   - Запустить `/analyze-dead-code`
   - Запустить `/detect-cycles`
   - Матиме весь контекст для рефакторингу
4. Cascade пропонує видалити мертвий код та розбити цикли

## 🔍 Як це працює

```
┌─────────────────────────────────────────────────────────────┐
│                    Codemap Analyzer                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. Сканує всі файли проєкту                              │
│     ↓                                                       │
│  2. Парсить код (Python, JS, TS, Go, Java)                │
│     ↓                                                       │
│  3. Будує граф залежностей (NetworkX)                     │
│     ↓                                                       │
│  4. Виявляє мертвий код (AST + regex)                     │
│     ↓                                                       │
│  5. Знаходить циклічні залежності                         │
│     ↓                                                       │
│  6. Генерує звіти (JSON, Markdown, HTML)                  │
│     ↓                                                       │
│  7. Поміщає в reports/                                    │
│     ↓                                                       │
│  8. Cascade читає CODEMAP_SUMMARY.md                      │
│     ↓                                                       │
│  9. Cascade має 100% контекст для аналізу/рефакторингу    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 📦 Залежності

```
ast-parse==1.0.0      # Парсинг Python AST
networkx==3.2         # Граф залежностей
pyyaml==6.0          # Конфігурація
jinja2==3.1.2        # Шаблони звітів
pathspec==0.11.2     # Фільтрація файлів
```

## 🐛 Розв'язання проблем

### Проблема: "ModuleNotFoundError: No module named 'yaml'"

**Рішення:**
```bash
pip install -r requirements.txt
```

### Проблема: Аналізатор повільний на великих проєктах

**Рішення:** Виключи папки в `config.yaml`:
```yaml
exclude_paths:
  - "node_modules"
  - "__pycache__"
  - "dist"
  - "build"
  - "venv"
```

### Проблема: Cascade не бачить звіти

**Рішення:** Переконайся, що:
1. `reports/CODEMAP_SUMMARY.md` існує
2. Cascade має доступ до папки `reports/`
3. Запусти `/update-codemap` workflow

## 🚀 Розширення

### Додати нову мову

Відредагуй `codemap_analyzer.py`:

```python
def _analyze_file(self, file_path: Path):
    if file_path.suffix == ".go":
        self._analyze_go_file(file_path, content)
    elif file_path.suffix == ".java":
        self._analyze_java_file(file_path, content)
```

### Додати новий формат звіту

```python
def _generate_csv_report(self, summary: Dict[str, Any]):
    # Твій код для CSV
    pass
```

### Додати нове правило для мертвого коду

```python
def _detect_dead_code(self):
    # Твої нові правила
    pass
```

## 📞 Підтримка

Якщо щось не працює:

1. Перевір `config.yaml` — правильні шляхи?
2. Запусти з `--once` і перевір помилки
3. Переглянь `reports/codemap_analysis.json` — дані там?
4. Перевір, чи файли мають правильні розширення

## 📄 Ліцензія

MIT

---

**Готово! Твоя система аналізу коду для Windsurf Cascade працює 🚀**

Запусти:
```bash
python3 codemap_analyzer.py --once
```

Потім відкрий `reports/CODEMAP_SUMMARY.md` і покажи Cascade:
```
Ctrl+L → Покажи мне граф залежностей цього проєкту
```

Cascade матиме 100% контекст! 🎉
