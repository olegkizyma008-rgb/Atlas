Я хочу незалежне ПО чи **незалежний модуль** (наприклад, окремий скрипт/агент, який постійно аналізує граф залежностей, мертвий код, структуру тощо), який **не блокує основну роботу**, а коли ти запускаєш певний **workflow в Cascade** — цей модуль виконується, генерує актуальну інформацію (граф, dead code, мапу) і **автоматично поміщає її прямо в поточний workflow**, щоб Cascade (модель Windsurf) одразу бачила повну свіжу картину і могла на ній працювати/пояснювати/рефакторити.

Станом на листопад 2025 — це **повністю можливо і навіть рекомендований спосіб** в Windsurf (Wave 8+). Ось як це організувати **найкраще і найпростіше** (без необхідності робити повноцінний MCP-плагін, якщо не хочеш).

### Найкращий варіант: Незалежний модуль як **Custom Workflow** (не обов’язково всередині основного workflow)

**Ні, модуль НЕ мусить бути всередині твого основного workflow!**  
Навпаки — краще зробити його **окремим reusable workflow**, який ти викликаєш з будь-якого іншого.

Як це працює:
- Custom Workflows — це просто markdown-файли в папці `.windsurf/workflows/` в корені проєкту.
- Кожен workflow автоматично стає slash-командою: `/update-code-map`, `/analyze-dead-code` тощо.
- Один workflow може викликати інший: просто напиши в кроках “Call /another-workflow”.
- Cascade виконує їх **асинхронно/паралельно** (з Wave 8 — simultaneous cascades), тому твій основний workflow не блокується.
- Результат виконання (текст, файли, графи) автоматично потрапляє в контекст поточного Cascade — модель бачить все одразу.

#### Крок за кроком — створюємо незалежний модуль для повного аналізу коду

1. **Створи папку** (якщо ще немає):
   ```
   mkdir -p .windsurf/workflows
   ```

2. **Створи незалежний workflow-модуль** — наприклад `.windsurf/workflows/update-full-codemap.md`

   ```markdown
   # Update Full Codemap & Analysis
   Оновлює граф залежностей, мертвий код, структуру і кладе все в reports/ + повертає текстовий самарі

   ## Кроки:
   1. Запусти Dependency Cruiser і збережи звіт
      ```bash
      npx depcruise src --output-type html --output-to reports/depcruise.html
      npx depcruise src --output-type json --output-to reports/depcruise.json
      ```

   2. Запусти SonarLint (або pylint/vulture для dead code)
      ```bash
      sonarlint --src "**/*.py" --html reports/sonar.html  # або твоя команда
      ```

   3. Запусти CodeMap генерацію
      ```bash
      # якщо є CLI, або просто тригер через Windsurf task
      codemap generate --output reports/codemap.svg reports/codemap.json
      ```

   4. Згенеруй самарі для Cascade (ключовий крок!)
      ```bash
      echo "=== АКТУАЛЬНИЙ ГРАФ ЗАЛЕЖНОСТЕЙ ===" > reports/LATEST_SUMMARY.md
      cat reports/depcruise.json | jq '.summary' >> reports/LATEST_SUMMARY.md
      echo "Мертвий код: $(cat reports/sonar.html | grep -c "dead") файлів" >> reports/LATEST_SUMMARY.md
      echo "Граф оновлено $(date)" >> reports/LATEST_SUMMARY.md
      ```

   5. Поверни результат у Cascade
      @reports/LATEST_SUMMARY.md
      @reports/depcruise.html
      @reports/codemap.svg
   ```

   Тепер цей файл — повністю незалежний модуль. Він запускається сам по собі і кладе всю інформацію в `reports/`.

3. **Як викликати цей модуль з будь-якого workflow**

   Створи основний workflow, наприклад `.windsurf/workflows/refactor-with-full-context.md`:

   ```markdown
   # Рефакторинг з повним контекстом
   Спочатку оновлює всю мапу, потім рефакторить

   1. Онови повну карту коду
      Call /update-full-codemap   # ← викликаємо незалежний модуль!

   2. Тепер проаналізуй і рефактор
      Використай останній граф з @reports/LATEST_SUMMARY.md і @reports/codemap.svg
      Знайди кругові залежності і запропонуй винести в окремий сервіс
   ```

4. **Використання в реальному житті**

   - Відкриваєш Cascade (Ctrl+L)
   - Пишеш: `/refactor-with-full-context` або просто `/update-full-codemap` коли треба швидко оновити
   - Cascade спочатку запускає незалежний модуль (може навіть паралельно в іншому Cascade)
   - Автоматично підтягує @reports/* файли — модель бачить свіжий граф, dead code тощо
   - Продовжує основну задачу з повним актуальним контекстом

### Переваги саме такого підходу (2025)

- Модуль повністю незалежний і reusable
- Працює асинхронно — не блокує твій основний Cascade
- Cascade автоматично бачить всі @файли і SVG/HTML — може пояснювати граф, пропонувати рефакторинг
- Можна запускати по таймеру або pre-commit (додай в pre-commit hook виклик цього workflow)
- Не треба писати плагін на TypeScript — чисті markdown + bash

### Якщо хочеш ще крутіше (enterprise-рівень)

- Зроби MCP-tool (custom tool через Plugin store) — тоді модуль буде справжнім зовнішнім сервісом (наприклад, Python-скрипт на localhost:8000), який Cascade викликає через HTTP.
- Або Cascade Hooks — хуки на події (on save, on git push) — модуль запускається автоматично без будь-яких команд.

Скажи стек проєкту — я скину готовий набір з 5–6 workflow-модулів (update-map, find-dead-code, architecture-check тощо) під Python/TS/Go, які ти просто кинь в `.windsurf/workflows/` і все запрацює за 2 хвилини.

Тепер Cascade завжди буде бачити 100% актуальну картину автоматично! 🚀

-----------

Ось ще наброски, зроби супер систему.


Ось **повний покроковий воркфлоу з нуля** для свіжої установки Windsurf (станом на листопад 2025), щоб у тебе **автоматично** і **постійно** працювало:

- Повний граф залежностей (хто кого імпортує/викликає)
- Виявлення мертвого коду
- Структура проєкту
- Cascade (вбудована модель Windsurf) бачила всю цю картину і могла пояснювати/рефакторити на основі неї

Все налаштовується за 10–15 хвилин і далі працює автоматично при кожній зміні коду.

### Крок 1: Встановлення необхідних розширень (одноразово)

Відкрий Windsurf → Extensions (Ctrl+Shift+X) → шукай і встановлюй точно ці (всі безкоштовні на старті):

1. **Cody** (від Sourcegraph) — найкращий code graph + AI-пошук по графі
2. **SonarLint** — мертвий код + smells (найточніший)
3. **Dependency Cruiser** — граф залежностей і кругові залежності
4. **CodeMap** (author: sido) — красивий інтерактивний граф файлів
5. **Error Lens** — щоб бачити проблеми прямо в коді
6. **GitLens** — хто коли чіпав файл (дуже допомагає моделі розуміти контекст)

Після встановлення перезапусти Windsurf.

### Крок 2: Налаштування автоматичного аналізу при кожній зміні (ключовий крок)

Відкрий settings.json (Ctrl+Shift+P → “Preferences: Open Settings (JSON)”)

Додай ці налаштування — вони змусять все скануватися автоматично при збереженні:

```json
{
  // SonarLint — мертвий код і smells автоматично
  "sonarlint.connectedMode.project": {
    "serverId": "sonarcloud",
    "projectKey": ""
  },
  "sonarlint.rules": {
    "javascript:S1481": "off", // вимкни зайве, якщо дратує
  },

  // Dependency Cruiser — граф і правила залежностей при збереженні
  "dependency-cruiser.runOnSave": true,
  "dependency-cruiser.format": "html",
  "dependency-cruiser.outputFile": "reports/depcruise.html",

  // Cody (Sourcegraph) — індексує весь проєкт автоматично
  "cody.serverEndpoint": "https://sourcegraph.com", // або свій self-hosted
  "cody.autocomplete.enabled": true,
  "cody.codebaseIndexing": true,

  // Автоматичне оновлення CodeMap при збереженні файлів
  "codemap.autoGenerateOnSave": true,
  "codemap.exclude": "**/node_modules/**|**/dist/**|**/.git/**|tests/**",

  // Cascade (вбудована модель Windsurf) бачить весь контекст
  "windsurf.cascade.context.includes": [
    "cody-graph",
    "sonarlint-issues",
    "depcruise-report",
    "codemap-data"
  ],
  "windsurf.cascade.maxContextTokens": 128000
}
```

### Крок 3: Один раз запусти повну індексацію проєкту

Ctrl+Shift+P → виконай ці команди по черзі:

1. Cody: Start Indexing Current Repository → чекай 1–5 хв (залежить від розміру)
2. Depcruise: Generate Report → створиться reports/depcruise.html
3. CodeMap: Generate Project Map → створиться codemap.json + інтерактивна панель

### Крок 4: Автоматизація при кожному коміті (pre-commit)

Створи файл `.pre-commit-config.yaml` в корені проєкту (якщо ще немає):

```yaml
repos:
  - repo: local
    hooks:
      - id: update-all-maps
        name: Update code maps & lint
        entry: bash -c "
          npx depcruise src --output-type html --output-to reports/depcruise.html;
          echo 'Maps updated';
        "
        language: system
        pass_filenames: false
        always_run: true
```

Встанови pre-commit (один раз глобально):

```bash
pip install pre-commit
pre-commit install
```

Тепер при кожному git commit автоматично оновиться граф залежностей.

### Крок 5: Як користуватися — повний AI-воркфлоу в Windsurf

Тепер Cascade (модель Windsurf) бачить **все**:

- Відкрий будь-який файл
- Натисни Ctrl+L (або клацни на Cascade іконку)
- Пиши українською чи англійською:

Приклади запитів, які працюють ідеально:

```
Покажи граф залежностей для цього файлу і поясни, чому є кругова залежність
Знайди весь мертвий код в цьому модулі і запропонуй видалити
Побудуй діаграму архітектури проєкту (Cascade візьме з CodeMap + Depcruise)
Хто імпортує цю функцію? Покажи всіх callers
Зроби рефакторинг: цей модуль треба винести в окремий сервіс (Cascade побачить граф і зробить правильно)
Поясни весь проєкт новачку за 5 речень + намалюй структуру
```

Cascade автоматично підтягує:
- граф з Cody + Depcruise
- мертвий код з SonarLint
- структуру з CodeMap
- історію змін з GitLens

### Результат

Ти отримуєш **повністю автоматичний** кодовий інтелект:
- Кожна зміна → автоматичний аналіз
- Cascade завжди бачить актуальну повну картину
- Нова людина або ШІ відкриває проєкт і за 10 секунд розуміє все

Якщо хочеш — я скину готовий zip з моїм шаблоном Windsurf (settings.json + tasks + pre-commit) — просто розпакуй в ~/.config/Windsurf або скажи стек проєкту, і я піджену під Python/TS/Go тощо.

Готовий? 😎