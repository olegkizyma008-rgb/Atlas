# ❓ Часті запитання (FAQ)

## 🎯 Загальні запитання

### Q: Що таке Codemap Analyzer?

**A:** Це незалежна система для постійного аналізу кодових графів, виявлення мертвого коду та інтеграції з Windsurf Cascade. Вона автоматично:
- Будує граф залежностей
- Виявляє мертвий код
- Знаходить циклічні залежності
- Генерує звіти для Cascade

### Q: Чому це потрібно?

**A:** Cascade (модель Windsurf) працює краще, коли має повний контекст:
- Без контексту: "Рефакторити цей код" → Cascade гадає
- З контекстом: "Рефакторити цей код" + граф залежностей → Cascade знає точно

### Q: Чи це блокує мою роботу?

**A:** Ні! Аналізатор:
- Працює незалежно
- Не блокує редактор
- Можна запустити в окремому терміналі
- Результати автоматично потрапляють до Cascade

### Q: Чи це безпечно?

**A:** Так! Аналізатор:
- Тільки парсить код (не виконує)
- Не передає дані на сервер
- Все локально
- Звіти зберігаються в папці `reports/`

## 🚀 Запуск

### Q: Як запустити аналізатор?

**A:** Три способи:

1. **Один раз:**
   ```bash
   python3 codemap_analyzer.py --once
   ```

2. **Постійне спостереження:**
   ```bash
   python3 codemap_analyzer.py --watch
   ```

3. **Через Windsurf workflow:**
   ```
   Ctrl+L → /update-codemap
   ```

### Q: Як довго це займає?

**A:** Залежить від розміру проєкту:
- Малий проєкт (< 100 файлів): < 1 сек
- Середній проєкт (100-1000 файлів): 1-5 сек
- Великий проєкт (> 1000 файлів): 5-30 сек

### Q: Як прискорити?

**A:** Виключи папки в `config.yaml`:
```yaml
exclude_paths:
  - "node_modules"
  - "__pycache__"
  - "dist"
  - "build"
  - "venv"
```

## 📊 Звіти

### Q: Де знаходяться звіти?

**A:** У папці `reports/`:
- `CODEMAP_SUMMARY.md` — для Cascade
- `codemap_analysis.json` — повні дані
- `codemap_analysis.html` — HTML звіт

### Q: Як Cascade читає звіти?

**A:** Автоматично! Коли ти запускаєш workflow:
```
/update-codemap
```

Cascade отримує `@reports/CODEMAP_SUMMARY.md` в контексті.

### Q: Чи можна переглянути звіти вручну?

**A:** Так!

```bash
# Markdown
cat reports/CODEMAP_SUMMARY.md

# JSON
cat reports/codemap_analysis.json | jq

# HTML
open reports/codemap_analysis.html
```

### Q: Як часто оновлюються звіти?

**A:** Залежить від режиму:
- `--once`: один раз
- `--watch`: кожні 5 секунд (налаштовується в `config.yaml`)
- Pre-commit: при кожному коміті
- GitHub Actions: при кожному push

## 🔍 Аналіз

### Q: Як виявляється мертвий код?

**A:** Аналізатор:
1. Знаходить всі визначення функцій (AST для Python, regex для JS)
2. Знаходить всі виклики функцій
3. Порівнює: якщо функція визначена, але не викликається → мертвий код

**Обмеження:**
- Не виявляє динамічні виклики (`eval()`, `__getattr__()`)
- Не виявляє виклики через рефлексію
- Не виявляє експортовані функції (вони можуть використовуватися ззовні)

### Q: Як виявляються циклічні залежності?

**A:** Аналізатор:
1. Будує граф залежностей (хто кого імпортує)
2. Використовує алгоритм пошуку циклів (NetworkX)
3. Повертає перші 10 циклів

**Приклад:**
```
module_a → module_b → module_c → module_a
```

### Q: Чи можна налаштувати правила?

**A:** Так! Відредагуй `config.yaml`:

```yaml
dead_code_rules:
  unused_functions: true      # Шукати невикористовувані функції
  unused_variables: false     # Не шукати невикористовувані змінні
  unused_imports: true        # Шукати невикористовувані імпорти
  unused_private_methods: true # Шукати невикористовувані приватні методи

dependency_rules:
  detect_cycles: true         # Шукати цикли
  max_depth: 5                # Максимальна глибина графу
  complexity_threshold: 10    # Поріг складності
```

## 🌍 Мови

### Q: Які мови підтримуються?

**A:** Поточна версія:
- ✅ Python (AST парсинг)
- ✅ JavaScript (regex парсинг)
- ✅ TypeScript (regex парсинг)
- ✅ JSX/TSX (regex парсинг)

**Майбутні версії:**
- Go, Java, C#, Rust, PHP

### Q: Як додати нову мову?

**A:** Відредагуй `codemap_analyzer.py`:

```python
def _analyze_file(self, file_path: Path):
    if file_path.suffix == ".go":
        self._analyze_go_file(file_path, content)

def _analyze_go_file(self, file_path: Path, content: str):
    # Твій код для Go
    # Парсити імпорти, функції, виклики
    pass
```

## 🔧 Налаштування

### Q: Як налаштувати для мого проєкту?

**A:** Відредагуй `config.yaml`:

```yaml
project:
  name: "My Project"
  root: "./"  # Коренева папка

analysis:
  include_paths:
    - "src"   # Папки для аналізу
    - "lib"
  exclude_paths:
    - "node_modules"  # Папки для ігнорування
    - "__pycache__"
  file_extensions:
    - ".py"   # Розширення для аналізу
    - ".js"
```

### Q: Як змінити інтервал оновлення?

**A:** У `config.yaml`:

```yaml
output:
  watch_interval: 5  # Секунди (за замовчуванням 5)
```

### Q: Як змінити папку для звітів?

**A:** У `config.yaml`:

```yaml
output:
  reports_dir: "reports"  # Папка для звітів
```

## 🪟 Windsurf

### Q: Як використовувати з Windsurf?

**A:** Три способи:

1. **Workflows (рекомендовано):**
   ```
   Ctrl+L → /update-codemap
   ```

2. **Ручне запитання:**
   ```
   Ctrl+L → Покажи граф залежностей
   @reports/CODEMAP_SUMMARY.md
   ```

3. **Pre-commit (автоматично):**
   ```bash
   pre-commit install
   ```

### Q: Чи можна запускати workflows паралельно?

**A:** Так! Windsurf підтримує паралельні workflows (Wave 8+).

### Q: Як додати новий workflow?

**A:** Створи файл в `.windsurf/workflows/`:

```markdown
# My Custom Workflow

Description

## Кроки
1. Запусти аналізатор
   ```bash
   python3 codemap_analyzer.py --once
   ```

2. Прочитай звіт
   @reports/CODEMAP_SUMMARY.md
```

## 🐛 Проблеми

### Q: Cascade не бачить звіти

**A:** Перевір:
1. Чи існує `reports/CODEMAP_SUMMARY.md`?
2. Чи запустив ти `/update-codemap`?
3. Чи папка `reports/` доступна?

**Рішення:**
```bash
python3 codemap_analyzer.py --once
ls reports/
```

### Q: Аналізатор повільний

**A:** Виключи папки:
```yaml
exclude_paths:
  - "node_modules"
  - "__pycache__"
  - "dist"
  - "build"
```

### Q: Неправильно виявляється мертвий код

**A:** Це нормально! Простий аналіз не може виявити:
- Динамічні виклики
- Рефлексію
- Експортовані функції

**Рішення:** Використовуй спеціалізовані інструменти:
- **Python**: `vulture`, `pylint`
- **JavaScript**: `eslint`, `jscpd`
- **TypeScript**: `tslint`, `ts-unused-exports`

### Q: Помилка "ModuleNotFoundError"

**A:** Встанови залежності:
```bash
pip install -r requirements.txt
```

### Q: Помилка "FileNotFoundError: config.yaml"

**A:** Переконайся, що `config.yaml` в корені проєкту:
```bash
ls config.yaml
```

Або вкажи шлях:
```bash
python3 codemap_analyzer.py --config /path/to/config.yaml
```

## 📈 Розширення

### Q: Як додати новий формат звіту?

**A:** Відредагуй `codemap_analyzer.py`:

```python
def _generate_csv_report(self, summary: Dict[str, Any]):
    report_path = self.reports_dir / "codemap_analysis.csv"
    # Твій код для CSV
    pass

# Додай в generate_reports()
if "csv" in formats:
    self._generate_csv_report(summary)
```

### Q: Як додати нове правило для мертвого коду?

**A:** Відредагуй `_detect_dead_code()`:

```python
def _detect_dead_code(self):
    # Твоє нове правило
    if rules.get("my_new_rule"):
        # Логіка
        pass
```

### Q: Як інтегрувати з CI/CD?

**A:** Приклад GitHub Actions:

```yaml
name: Update Codemap

on: [push]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
      - run: pip install -r requirements.txt
      - run: python3 codemap_analyzer.py --once
      - uses: actions/upload-artifact@v3
        with:
          name: codemap-reports
          path: reports/
```

## 🎓 Приклади

### Q: Як знайти всі циклічні залежності?

**A:**
```bash
python3 codemap_analyzer.py --once
cat reports/codemap_analysis.json | jq '.cycles'
```

### Q: Як знайти всі невикористовувані функції?

**A:**
```bash
python3 codemap_analyzer.py --once
cat reports/CODEMAP_SUMMARY.md | grep "Unused Functions" -A 20
```

### Q: Як рефакторити з контекстом?

**A:**
```
Ctrl+L → /refactor-with-context
```

Cascade:
1. Запустить `/update-codemap`
2. Запустить `/analyze-dead-code`
3. Запустить `/detect-cycles`
4. Матиме весь контекст для рефакторингу

## 💡 Поради

### Совет 1: Постійне спостереження

Запусти в окремому терміналі:
```bash
python3 codemap_analyzer.py --watch
```

Тепер звіти автоматично оновлюються при кожній зміні.

### Совет 2: Pre-commit Hook

Встанови:
```bash
pip install pre-commit
pre-commit install
```

Звіти автоматично оновляться при кожному коміті.

### Совет 3: Регулярний аналіз

Додай в crontab (Linux/Mac):
```bash
0 9 * * * cd /path/to/project && python3 codemap_analyzer.py --once
```

Аналіз запускатиметься кожного дня о 9:00.

### Совет 4: Інтеграція з IDE

Додай в VS Code tasks.json:
```json
{
  "label": "Update Codemap",
  "type": "shell",
  "command": "python3 codemap_analyzer.py --once",
  "group": "build"
}
```

Тепер можна запустити через Ctrl+Shift+B.

## 📞 Контакти

Якщо щось не працює або є питання:
1. Перевір документацію в `README.md`
2. Перевір конфігурацію в `config.yaml`
3. Запусти з `--once` і перевір помилки
4. Переглянь `reports/codemap_analysis.json`

---

**Готово! Твоя система аналізу коду для Windsurf працює! 🚀**
