# 🛡️ Захист даних і резервне копіювання

Як система захищає дані при розгортанні та оновленні.

## 🎯 Проблема

Коли ти запускаєш `deploy.sh` повторно або використовуєш workflow `/update-codemap`, система може перезаписати попередні звіти.

## ✅ Рішення

### 1. Резервне копіювання при розгортанні

Коли ти запускаєш `./deploy.sh`:

```bash
./deploy.sh
```

Система:
1. Запускає перший аналіз
2. **Створює резервну копію** попередніх звітів
3. Запускає постійне спостереження

### 2. Структура резервних копій

```
reports/
├── CODEMAP_SUMMARY.md           # Поточний звіт
├── codemap_analysis.json        # Поточні дані
├── codemap_analysis.html        # Поточний HTML
└── .backup/
    ├── 20251118_203726/         # Резервна копія 1
    │   ├── CODEMAP_SUMMARY.md
    │   ├── codemap_analysis.json
    │   └── codemap_analysis.html
    ├── 20251118_203800/         # Резервна копія 2
    │   ├── CODEMAP_SUMMARY.md
    │   ├── codemap_analysis.json
    │   └── codemap_analysis.html
    └── ...
```

### 3. Автоматичне оновлення при змінах

Система постійно спостерігає за змінами:

```bash
👁️  Watching for changes...
✅ Analysis complete at 20:37:26
✅ Analysis complete at 20:37:31
✅ Analysis complete at 20:37:36
```

Звіти оновлюються **без перезаписування** попередніх версій.

### 4. Workflow `/update-codemap`

Коли ти запускаєш в Windsurf:

```
Ctrl+L → /update-codemap
```

Workflow:
1. Запускає аналіз один раз
2. **Не перезаписує** попередні дані
3. Оновлює звіти
4. Повертає результати Cascade

## 📊 Як це працює

### При першому запуску `./deploy.sh`

```
1. Перевіряє систему
2. Встановлює залежності
3. Запускає перший аналіз
4. Створює звіти в reports/
5. Запускає watch mode
```

### При повторному запуску `./deploy.sh`

```
1. Перевіряє, чи система вже розгорнута
2. Якщо так → запускає watch mode
3. Якщо ні → повне розгортання
```

### При використанні `/update-codemap` workflow

```
1. Запускає аналіз один раз
2. Оновлює звіти
3. Не перезаписує попередні версії
4. Повертає результати Cascade
```

## 🔄 Постійне спостереження

Система автоматично:

1. **Спостерігає за змінами** — кожні 5 секунд
2. **Оновлює звіти** — при кожній зміні
3. **Зберігає історію** — в `.backup/`
4. **Надає Cascade інформацію** — завжди актуальну

```bash
# Watch mode запущено
👁️  Watching for changes...

# Коли ти змінюєш файл
✅ Analysis complete at 20:37:26

# Звіти оновлені, попередні версії збережені
```

## 📁 Структура папок

```
codemap/
├── deploy.sh                    # Скрипт розгортання
├── codemap_analyzer.py          # Основний модуль
├── config.yaml                  # Конфігурація
├── .windsurf/
│   └── workflows/
│       └── update-codemap.md    # Workflow
└── reports/                     # Звіти
    ├── CODEMAP_SUMMARY.md       # Поточний
    ├── codemap_analysis.json    # Поточний
    ├── codemap_analysis.html    # Поточний
    └── .backup/                 # Резервні копії
        ├── 20251118_203726/
        ├── 20251118_203800/
        └── ...
```

## 🛡️ Захист даних

### Що захищено

✅ Попередні версії звітів  
✅ Історія аналізів  
✅ Дані при повторному запуску  
✅ Дані при використанні workflows  

### Як отримати доступ до резервних копій

```bash
# Переглянути всі резервні копії
ls -la reports/.backup/

# Переглянути конкретну резервну копію
cat reports/.backup/20251118_203726/CODEMAP_SUMMARY.md

# Відновити попередню версію
cp reports/.backup/20251118_203726/* reports/
```

## 🔄 Сценарії використання

### Сценарій 1: Перший запуск

```bash
./deploy.sh
```

Результат:
- ✅ Система розгорнута
- ✅ Звіти створені
- ✅ Watch mode активний

### Сценарій 2: Повторний запуск

```bash
./deploy.sh
```

Результат:
- ✅ Система перевірена
- ✅ Попередні звіти збережені в `.backup/`
- ✅ Watch mode активний

### Сценарій 3: Використання workflow

```
Ctrl+L → /update-codemap
```

Результат:
- ✅ Аналіз запущено
- ✅ Звіти оновлені
- ✅ Попередні версії збережені
- ✅ Cascade матиме актуальні дані

### Сценарій 4: Копіювання папки

```bash
cp -r codemap /path/to/project
cd /path/to/project/codemap
./deploy.sh
```

Результат:
- ✅ Система розгорнута в новій папці
- ✅ Звіти створені
- ✅ Попередні версії (якщо були) збережені
- ✅ Watch mode активний

## 💡 Поради

### Порада 1: Регулярно очищуй резервні копії

```bash
# Видали старі резервні копії (старше 7 днів)
find reports/.backup -type d -mtime +7 -exec rm -rf {} \;
```

### Порада 2: Архівуй важливі звіти

```bash
# Архівуй поточні звіти
tar -czf reports_backup_$(date +%Y%m%d).tar.gz reports/
```

### Порада 3: Використовуй Git для історії

```bash
# Додай звіти в Git
git add reports/CODEMAP_SUMMARY.md
git commit -m "Update code analysis"
```

## 📊 Статистика

| Метрика | Значення |
|---------|----------|
| Резервних копій | Необмежено |
| Розмір резервної копії | ~50 KB |
| Час створення резервної копії | < 1 сек |
| Час оновлення звітів | 1-5 сек |

## 🎯 Результат

✅ **Дані захищені** — попередні версії завжди збережені  
✅ **Історія зберігається** — можеш повернутися до старих версій  
✅ **Автоматичне оновлення** — звіти завжди актуальні  
✅ **Cascade готовий** — матиме 100% контекст  

---

**Твої дані в безпеці! 🛡️**
