# 🏗️ Гайд Системи Архітектури

**Статус**: ✅ ПРАЦЮЄ  
**Дата**: 21 листопада 2025

---

## ✅ Система Працює

Архітектурна система **повністю функціональна** і генерує аналіз в реальному часі.

---

## 📁 Розташування Файлів

### Інструменти
```
codemap-system/
├── architecture_mapper.py          # Рекурсивний аналіз архітектури
└── mcp_architecture_tools.py       # MCP інструменти для Windsurf
```

### Звіти та Результати
```
codemap-system/reports/
├── architecture_map.json           # Основна архітектурна карта (509 KB)
├── architecture_detailed_report.json  # Детальний звіт (генерується)
├── DASHBOARD.html                  # Інтерактивна панель
└── README.md                       # Документація
```

### Документація
```
docs/
└── ARCHITECTURE_SYSTEM.md          # Повна документація системи
```

---

## 🚀 Як Запустити

### 1️⃣ Базовий Аналіз

```bash
cd codemap-system
python architecture_mapper.py
```

**Результат**:
- Генерує `architecture_map.json`
- Виводить звіт в консоль
- Показує статистику та здоров'я архітектури

### 2️⃣ Отримати Метрики

```bash
cd /Users/dev/Documents/GitHub/atlas4
python << 'PYTHON'
import sys
sys.path.insert(0, 'codemap-system')
from mcp_architecture_tools import MCPArchitectureTools
from pathlib import Path

tools = MCPArchitectureTools(Path('.'))
metrics = tools.get_architecture_metrics()

print("📊 МЕТРИКИ:")
print(f"  • Файлів: {metrics['statistics']['total_files']}")
print(f"  • Активних: {metrics['statistics']['active_files']}")
print(f"  • Невикористовуваних: {metrics['statistics']['unused_files']}")
print(f"  • Оцінка здоров'я: {metrics['health']['score']:.1f}/100")
PYTHON
```

### 3️⃣ Експортувати Детальний Звіт

```bash
cd /Users/dev/Documents/GitHub/atlas4
python << 'PYTHON'
import sys
sys.path.insert(0, 'codemap-system')
from mcp_architecture_tools import MCPArchitectureTools
from pathlib import Path

tools = MCPArchitectureTools(Path('.'))
output = Path('codemap-system/reports/architecture_detailed_report.json')
tools.export_architecture_report(output)
print(f"✅ Звіт експортований в {output}")
PYTHON
```

### 4️⃣ Отримати Граф Залежностей

```bash
cd /Users/dev/Documents/GitHub/atlas4
python << 'PYTHON'
import sys
sys.path.insert(0, 'codemap-system')
from mcp_architecture_tools import MCPArchitectureTools
from pathlib import Path

tools = MCPArchitectureTools(Path('.'))

# Граф для конкретного файлу
graph = tools.get_dependency_graph(
    'orchestrator/workflow/executor-v3.js',
    depth=2
)

print(f"📊 Граф залежностей:")
print(f"  • Вузлів: {len(graph['nodes'])}")
print(f"  • Ребер: {len(graph['edges'])}")
PYTHON
```

### 5️⃣ Знайти Невикористовувані Файли

```bash
cd /Users/dev/Documents/GitHub/atlas4
python << 'PYTHON'
import sys
sys.path.insert(0, 'codemap-system')
from mcp_architecture_tools import MCPArchitectureTools
from pathlib import Path

tools = MCPArchitectureTools(Path('.'))
unused = tools.find_unused_files()

print(f"🗑️ Невикористовувані файли ({len(unused)}):")
for f in unused[:10]:
    print(f"  • {f['path']} ({f['lines']} рядків)")
PYTHON
```

---

## 📊 Поточні Результати

### Архітектура
```
✅ Всього файлів: 167
✅ Активних: 139
⚠️ Невикористовуваних: 28
✅ Оцінка здоров'я: 100.0/100
✅ Модульність: good
```

### Статистика Коду
```
📝 Всього рядків: 65,167
📏 Середня довжина файлу: 390 рядків
💾 Загальний розмір: ~2.4 MB
```

### Залежності
```
🔗 Файлів з залежностями: 98
📊 Середня залежність: 2.7
```

---

## 🔍 Що Аналізує Система

### Для Кожного Файлу
- ✅ Розмір та кількість рядків
- ✅ Імпорти та експорти
- ✅ Функції та класи
- ✅ Залежності
- ✅ Залежні файли
- ✅ Статус (ACTIVE, UNUSED, DEPRECATED, LEGACY, EXPERIMENTAL)
- ✅ Оцінка здоров'я (0-100)

### Для Архітектури
- ✅ Загальна статистика
- ✅ Граф залежностей
- ✅ Оцінка здоров'я (0-100)
- ✅ Рекомендації для поліпшення
- ✅ Невикористовувані файли
- ✅ Файли з високою зв'язністю

---

## 💡 Рекомендації

Система автоматично генерує рекомендації:

```
🗑️ Видалити 28 невикористовуваних файлів
📏 Розділити великі файли на менші модулі
```

---

## 🔗 Інтеграція з Windsurf

Windsurf може використовувати архітектуру для:

1. **Контекстної свідомості** - знати структуру перед змінами
2. **Прийняття рішень** - вибирати найкращі місця для коду
3. **Рефакторингу** - пропонувати поліпшення
4. **Тестування** - розуміти залежності для тестів
5. **Документування** - генерувати документацію на основі архітектури

---

## 📈 Як Система Працює

```
1. Знаходить всі файли workflow
   ↓
2. Аналізує кожен файл (імпорти, експорти, функції, класи)
   ↓
3. Розв'язує залежності (рекурсивно на глибину 5)
   ↓
4. Визначає статуси файлів
   ↓
5. Розраховує метрики здоров'я
   ↓
6. Генерує рекомендації
   ↓
7. Експортує результати в JSON
```

---

## 🎯 Приклади Використання

### Приклад 1: Перевірити здоров'я конкретного файлу

```python
tools = MCPArchitectureTools(Path('.'))
status = tools.get_file_status('orchestrator/workflow/executor-v3.js')
print(f"Статус: {status['status']}")
print(f"Залежностей: {len(status['dependencies'])}")
print(f"Залежних: {len(status['dependents'])}")
print(f"Здоров'я: {status['health']['score']:.1f}/100")
```

### Приклад 2: Знайти проблемні файли

```python
tools = MCPArchitectureTools(Path('.'))

# Невикористовувані
unused = tools.find_unused_files()
print(f"Видалити: {len(unused)} файлів")

# З високою зв'язністю
high_coupling = tools.find_high_coupling_files(threshold=5)
print(f"Розділити: {len(high_coupling)} файлів")
```

### Приклад 3: Отримати граф залежностей

```python
tools = MCPArchitectureTools(Path('.'))
graph = tools.get_dependency_graph('path/to/file.js', depth=3)

for node, info in graph['nodes'].items():
    print(f"{node}: {info['status']} ({info['lines']} рядків)")

for edge in graph['edges']:
    print(f"{edge['from']} -> {edge['to']}")
```

---

## 📝 Висновок

✅ **Система архітектури повністю працює**

- Генерує детальну карту залежностей
- Розраховує метрики здоров'я
- Пропонує рекомендації
- Готова для інтеграції з Windsurf

**Файли знаходяться в**:
- `codemap-system/architecture_mapper.py`
- `codemap-system/mcp_architecture_tools.py`
- `codemap-system/reports/architecture_map.json`

**Запустити**: `python codemap-system/architecture_mapper.py`
