---
description: Повний аналіз архітектури проекту з максимальною глибиною
---

# 🏗️ Повний Аналіз Архітектури

Цей воркфлоу виконує комплексний аналіз архітектури проекту з максимальною глибиною.

## Кроки

### 1. Отримати огляд архітектури
// turbo
Запустіть MCP інструмент для отримання загального огляду:
```bash
mcp0_get_architecture_overview
```

Це покаже:
- Загальну статистику файлів
- Розподіл за статусом (ACTIVE, UNUSED, IN_DEVELOPMENT)
- Оцінку здоров'я архітектури
- Кількість циклічних залежностей

### 2. Перевірити здоров'я архітектури
// turbo
```bash
mcp0_get_architecture_health
```

Це покаже:
- Оцінку здоров'я (0-100)
- Коефіцієнт невикористовуваних файлів
- Середню кількість залежностей
- Рекомендації для покращення

### 3. Виявити циклічні залежності
// turbo
```bash
mcp0_detect_circular_dependencies
```

Це покаже:
- Список циклічних залежностей (якщо є)
- Кількість циклів
- Файли, які беруть участь у циклах

### 4. Виявити невикористовувані файли
// turbo
```bash
mcp0_detect_unused_files
```

Це покаже:
- Список невикористовуваних файлів
- Розмір кожного файлу
- Кількість рядків коду

### 5. Виявити дублікати коду
// turbo
```bash
mcp0_detect_duplicates
<parameter name="min_lines">5
