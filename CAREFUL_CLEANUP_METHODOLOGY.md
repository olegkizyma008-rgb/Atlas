# 🎯 Методологія Акуратної Чистки Коду

**Дата**: 2025-11-19 02:05 UTC+02:00  
**Версія**: 1.0  
**Статус**: ✅ ЗАТВЕРДЖЕНО  

---

## 🚨 КРИТИЧНЕ УРАХУВАННЯ

### Помилка Попереднього Аналізу

Codemap виявив **3,950 невикористаних функцій**, але це **НЕПРАВИЛЬНА КЛАСИФІКАЦІЯ**.

Насправді:
- ❌ **3,900 з них** - це невикористані ЗМІННІ та ЛОКАЛЬНІ ФУНКЦІЇ всередину методів
- ✅ **Весь файл** `mcp-todo-manager.js` - активно використовуваний компонент
- ✅ **Всі експортовані методи** - критичні для системи

---

## 📋 ПРАВИЛЬНА МЕТОДОЛОГІЯ

### Крок 1: Перевірка Залежностей

**Перед видаленням БУДЬ-ЯКОГО коду:**

```bash
# Пошук всіх імпортів файлу
grep -r "import.*from.*mcp-todo-manager" .

# Пошук всіх використань класу
grep -r "MCPTodoManager" .

# Пошук всіх використань методів
grep -r "mcpTodoManager\." .

# Пошук в DI Container
grep -r "mcpTodoManager" orchestrator/core/service-registry.js
```

### Крок 2: Аналіз Контексту

**Для кожного "мертвого" елемента:**

1. **Визначити тип**:
   - Це експортована функція/метод? → НЕ ВИДАЛЯТИ
   - Це локальна змінна? → Можна видалити
   - Це параметр методу? → Перевірити, чи використовується

2. **Перевірити використання**:
   ```bash
   # Пошук використання змінної
   grep -n "openBraces" orchestrator/workflow/mcp-todo-manager.js
   
   # Пошук в інших файлах
   grep -r "openBraces" orchestrator/
   ```

3. **Перевірити контекст**:
   - Це залишок рефакторингу?
   - Це для майбутнього розширення?
   - Це справді мертвий код?

### Крок 3: Безпечне Видалення

**Видаляти ТІЛЬКИ:**
- Локальні змінні, які не використовуються
- Локальні функції, які не викликаються
- Невикористані параметри (з обережністю)

**НЕ видаляти:**
- Експортовані функції/методи
- Параметри методів (можуть бути для API сумісності)
- Код, який викликається динамічно

### Крок 4: Тестування

**Після кожного видалення:**

```bash
# Запустити тести
npm test

# Запустити linter
npm run lint

# Запустити сервер
npm start

# Перевірити функціональність
curl http://localhost:3000/health
```

---

## 🔍 ПРИКЛАД: MCPTodoManager

### Аналіз Невикористаних Змінних

#### Змінна: `openBraces` (рядок 2752)

```javascript
// mcp-todo-manager.js:2752
const openBraces = '{';  // ← Невикористана змінна
```

**Аналіз**:
1. Це локальна змінна (не експортована) ✓
2. Пошук використання:
   ```bash
   grep -n "openBraces" mcp-todo-manager.js
   # Результат: тільки оголошення, без використання
   ```
3. Контекст: Залишок рефакторингу (парсинг JSON)
4. **Висновок**: Можна видалити ✓

#### Параметр: `params` (рядок 3585)

```javascript
// mcp-todo-manager.js:3585
async _someMethod(params) {  // ← Невикористаний параметр
  // params не використовується в методі
}
```

**Аналіз**:
1. Це параметр методу (приватної функції) ✓
2. Пошук використання:
   ```bash
   grep -n "params" mcp-todo-manager.js | grep -A 20 "_someMethod"
   # Результат: параметр не використовується
   ```
3. Контекст: Можливо для майбутнього розширення
4. **Висновок**: Видалити з обережністю (перевірити, чи це API)

#### Локальна функція: `isPythonModuleTask` (рядок 3588)

```javascript
// mcp-todo-manager.js:3588
function isPythonModuleTask(item) {  // ← Невикористана функція
  // ...
}
```

**Аналіз**:
1. Це локальна функція (не експортована) ✓
2. Пошук використання:
   ```bash
   grep -n "isPythonModuleTask" mcp-todo-manager.js
   # Результат: тільки оголошення, без викликів
   ```
3. Контекст: Залишок рефакторингу (перевірка типу завдання)
4. **Висновок**: Можна видалити ✓

---

## ✅ ЧЕК-ЛИСТ ПЕРЕД ВИДАЛЕННЯМ

### Для кожного елемента:

- [ ] Це експортована функція/метод? (НЕ видаляти)
- [ ] Це використовується в інших файлах? (НЕ видаляти)
- [ ] Це використовується в DI Container? (НЕ видаляти)
- [ ] Це параметр методу? (Перевірити API)
- [ ] Це локальна змінна? (Можна видалити)
- [ ] Це локальна функція? (Можна видалити)
- [ ] Це залишок рефакторингу? (Видалити)
- [ ] Це для майбутнього розширення? (Залишити коментар)
- [ ] Тести проходять? (Перевірити)
- [ ] Сервер запускається? (Перевірити)

---

## 🚀 РЕКОМЕНДОВАНИЙ ПОРЯДОК ДІЙ

### Фаза 1: Аналіз (1-2 години)

1. Запустити Codemap аналіз
2. Для кожного "мертвого" елемента:
   - Визначити тип
   - Перевірити використання
   - Перевірити контекст
3. Створити список для видалення
4. Перевірити список з team lead

### Фаза 2: Видалення (2-3 години)

1. Видалити елементи по одному
2. Запустити тести після кожного видалення
3. Перевірити, що функціональність не порушена
4. Коміт для кожного видалення

### Фаза 3: Валідація (1-2 години)

1. Запустити всі тести
2. Запустити сервер
3. Перевірити всі workflows
4. Запустити Codemap аналіз знову

---

## 🎯 ІНСТРУМЕНТИ ДЛЯ АНАЛІЗУ

### 1. Grep для пошуку використання

```bash
# Пошук всіх використань змінної
grep -rn "openBraces" orchestrator/

# Пошук в конкретному файлі
grep -n "openBraces" orchestrator/workflow/mcp-todo-manager.js

# Пошук з контекстом
grep -B 2 -A 2 "openBraces" orchestrator/workflow/mcp-todo-manager.js
```

### 2. Codemap для аналізу залежностей

```bash
cd codemap-system
python3 codemap_analyzer.py --once

# Перевірити звіти
cat reports/CODEMAP_SUMMARY.md
cat reports/codemap_analysis.json | jq '.file_imports."orchestrator/workflow/mcp-todo-manager.js"'
```

### 3. ESLint для виявлення невикористаних змінних

```bash
npm run lint -- orchestrator/workflow/mcp-todo-manager.js

# Або встановити ESLint правило
npm install --save-dev eslint-plugin-unused-imports
```

### 4. IDE для пошуку посилань

- VS Code: `Ctrl+Shift+F` для пошуку
- VS Code: `Ctrl+F2` для пошуку всіх посилань
- WebStorm: `Ctrl+Shift+F` для пошуку

---

## 🔒 БЕЗПЕЧНІ ПРАКТИКИ

### 1. Завжди перевіряйте залежності

```bash
# Перед видаленням файлу
grep -r "import.*from.*file-name" .
grep -r "require.*file-name" .
```

### 2. Запускайте тести

```bash
# Перед видаленням
npm test

# Після видалення
npm test

# Порівняйте результати
```

### 3. Використовуйте версійний контроль

```bash
# Створіть branch
git checkout -b cleanup/remove-dead-code

# Коміт для кожного видалення
git add file.js
git commit -m "refactor: remove unused variable X from file.js"

# Легко повернути, якщо щось пішло не так
git revert <commit-hash>
```

### 4. Документуйте причину

```javascript
// ❌ Неправильно
// Видалити цю змінну
const unusedVar = 'test';

// ✅ Правильно
// REMOVED 2025-11-19: Unused variable from legacy parser
// const unusedVar = 'test';
```

---

## 📊 МЕТРИКИ УСПІХУ

### Перед чисткою:
- Невикористаних змінних: ~15
- Невикористаних локальних функцій: ~5
- Невикористаних параметрів: ~10
- Розмір файлу: 4,000+ рядків

### Після чистки:
- Невикористаних змінних: 0
- Невикористаних локальних функцій: 0
- Невикористаних параметрів: 0
- Розмір файлу: 3,970+ рядків (-30 рядків)
- Функціональність: 100% збережена

---

## 🎉 ВИСНОВОК

**Правильна методологія чистки коду:**

1. ✅ Перевіряйте залежності перед видаленням
2. ✅ Аналізуйте контекст кожного елемента
3. ✅ Видаляйте тільки справді мертвий код
4. ✅ Запускайте тести після кожного видалення
5. ✅ Документуйте причину видалення
6. ✅ Використовуйте версійний контроль

**Неправильна методологія:**

1. ❌ Видаляйте без перевірки залежностей
2. ❌ Видаляйте експортовані функції
3. ❌ Видаляйте без тестування
4. ❌ Видаляйте без документування
5. ❌ Видаляйте без версійного контролю

---

**Статус**: ✅ ЗАТВЕРДЖЕНО  
**Автор**: Cascade AI + MCP Codemap  
**Дата**: 2025-11-19 02:05 UTC+02:00
