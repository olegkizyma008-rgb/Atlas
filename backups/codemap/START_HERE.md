# 🚀 START HERE - Почни звідси!

Ласкаво просимо до **Codemap Analyzer** — системи для аналізу кодових графів та інтеграції з Windsurf Cascade!

## ⚡ За 30 секунд

### Один скрипт - все автоматично!

```bash
# Скопіюй папку
cp -r codemap /path/to/your/project

# Перейди в папку
cd /path/to/your/project/codemap

# Запусти розгортання
./deploy.sh
```

**Готово!** Система розгорнута, аналіз запущено, постійне спостереження активне.

### Що буде:
- ✅ Встановлені залежності
- ✅ Запущено перший аналіз
- ✅ Створені звіти в `reports/`
- ✅ **Автоматично оновлені workflows** (якщо потрібно)
- ✅ Налаштовано Windsurf
- ✅ Запущено постійне спостереження

### Windsurf Cascade (автоматично)

Після розгортання система автоматично готова для Windsurf:

```
Ctrl+L → /update-codemap
```

**Що буде:**
- ✅ Аналіз запущено
- ✅ Звіти оновлені
- ✅ Попередні версії збережені в `reports/.backup/`
- ✅ Cascade матиме 100% контекст

**Інші workflows:**
- `/analyze-dead-code` — знайти мертвий код
- `/detect-cycles` — виявити циклічні залежності
- `/refactor-with-context` — рефакторити з контекстом

## 📚 Документація

Залежно від того, що тебе цікавить:

| Якщо ти... | Прочитай |
|-----------|---------|
| Новачок | [`QUICKSTART.md`](QUICKSTART.md) — 5 хвилин |
| Встановлюєш | [`INSTALL.md`](INSTALL.md) — крок за кроком |
| Налаштовуєш | [`config.yaml`](config.yaml) — конфігурація |
| Інтегруєш з Windsurf | [`INTEGRATION_GUIDE.md`](INTEGRATION_GUIDE.md) |
| Хочеш приклади | [`EXAMPLES.md`](EXAMPLES.md) — 12 прикладів |
| Цікавить архітектура | [`ARCHITECTURE.md`](ARCHITECTURE.md) |
| Є запитання | [`FAQ.md`](FAQ.md) — часті запитання |
| Хочеш огляд | [`README.md`](README.md) — повна документація |

## 🎯 Основні можливості

### 1. Граф залежностей

Аналізатор будує повний граф того, як модулі залежать один від одного:

```
module_a → module_b → module_c
```

### 2. Виявлення мертвого коду

Знаходить функції, які ніхто не викликає:

```python
def unused_function():  # ← Мертвий код!
    return "not used"
```

### 3. Циклічні залежності

Виявляє проблемні залежності:

```
module_a → module_b → module_a  # ← Цикл!
```

### 4. Метрики складності

Розраховує метрики архітектури:

```
- Average imports per file: 3.5
- Average functions per file: 4.2
- Max dependency depth: 5
```

## 🪟 Інтеграція з Windsurf

### Способ 1: Workflows (найпростіше)

```
Ctrl+L → /update-codemap
```

Cascade запустить аналізатор і отримає всю інформацію.

### Способ 2: Ручне запитання

```
Ctrl+L → Покажи граф залежностей
@reports/CODEMAP_SUMMARY.md
```

### Способ 3: Pre-commit (автоматично)

```bash
pre-commit install
git commit -m "My changes"  # Автоматично запустить аналізатор
```

## 🔧 Налаштування для твого проєкту

Відредагуй `config.yaml`:

```yaml
project:
  name: "My Project"
  root: "./"  # ← Твоя папка проєкту

analysis:
  include_paths:
    - "src"      # ← Папки з кодом
    - "lib"
  exclude_paths:
    - "node_modules"  # ← Папки для ігнорування
    - "__pycache__"
```

## 📊 Звіти

Аналізатор генерує три типи звітів:

1. **`CODEMAP_SUMMARY.md`** — для Cascade
   ```bash
   cat reports/CODEMAP_SUMMARY.md
   ```

2. **`codemap_analysis.json`** — повні дані
   ```bash
   cat reports/codemap_analysis.json | jq
   ```

3. **`codemap_analysis.html`** — HTML звіт
   ```bash
   open reports/codemap_analysis.html
   ```

## 🚀 Режими роботи

### Один раз
```bash
python3 codemap_analyzer.py --once
```

### Постійне спостереження (автоматичне оновлення)
```bash
python3 codemap_analyzer.py --watch
```

### З конфігом
```bash
python3 codemap_analyzer.py --config /path/to/config.yaml --once
```

## 🎓 Приклади

### Приклад 1: Знайти мертвий код

```bash
python3 codemap_analyzer.py --once
cat reports/CODEMAP_SUMMARY.md | grep "Unused Functions" -A 20
```

### Приклад 2: Виявити циклічні залежності

```bash
python3 codemap_analyzer.py --once
cat reports/codemap_analysis.json | jq '.cycles'
```

### Приклад 3: Рефакторити з контекстом

```
Ctrl+L → /refactor-with-context
```

Cascade матиме весь контекст для рефакторингу!

## ❓ Часті запитання

**Q: Чи це блокує мою роботу?**  
A: Ні! Аналізатор працює незалежно.

**Q: Як часто оновлюються звіти?**  
A: Залежить від режиму: `--once` (один раз), `--watch` (кожні 5 сек), pre-commit (при коміті).

**Q: Які мови підтримуються?**  
A: Python (AST), JavaScript/TypeScript (regex). Майбутні версії: Go, Java, C#, Rust.

**Q: Як додати нову мову?**  
A: Відредагуй `codemap_analyzer.py` і додай метод `_analyze_<language>_file()`.

**Більше питань?** Див. [`FAQ.md`](FAQ.md)

## 🐛 Розв'язання проблем

### Проблема: "ModuleNotFoundError: No module named 'yaml'"

```bash
pip install -r requirements.txt
```

### Проблема: Cascade не бачить звіти

```bash
python3 codemap_analyzer.py --once
ls reports/
```

### Проблема: Аналізатор повільний

Виключи папки в `config.yaml`:
```yaml
exclude_paths:
  - "node_modules"
  - "__pycache__"
  - "dist"
```

**Більше проблем?** Див. [`INSTALL.md`](INSTALL.md)

## 📋 Чек-лист першого запуску

- [ ] Встановлено Python 3.8+
- [ ] Встановлено залежності (`pip install -r requirements.txt`)
- [ ] Налаштовано `config.yaml`
- [ ] Запущено аналізатор (`python3 codemap_analyzer.py --once`)
- [ ] Переглянуто звіти (`cat reports/CODEMAP_SUMMARY.md`)
- [ ] Тестовано з Windsurf (`Ctrl+L → /update-codemap`)

## 🎉 Готово!

Твоя система аналізу коду для Windsurf Cascade готова!

### Наступні кроки:

1. **Запусти аналіз:**
   ```bash
   python3 codemap_analyzer.py --once
   ```

2. **Переглянь результати:**
   ```bash
   cat reports/CODEMAP_SUMMARY.md
   ```

3. **Використовуй з Windsurf:**
   ```
   Ctrl+L → /update-codemap
   ```

4. **Налаштуй автоматизацію:**
   ```bash
   pre-commit install
   ```

## 📚 Повна документація

- [`README.md`](README.md) — основна документація
- [`QUICKSTART.md`](QUICKSTART.md) — швидкий старт
- [`INSTALL.md`](INSTALL.md) — встановлення
- [`INTEGRATION_GUIDE.md`](INTEGRATION_GUIDE.md) — інтеграція з Windsurf
- [`ARCHITECTURE.md`](ARCHITECTURE.md) — архітектура
- [`FAQ.md`](FAQ.md) — часті запитання
- [`EXAMPLES.md`](EXAMPLES.md) — приклади
- [`PROJECT_SUMMARY.md`](PROJECT_SUMMARY.md) — резюме проєкту

## 💡 Поради

1. **Постійне спостереження:**
   ```bash
   python3 codemap_analyzer.py --watch
   ```
   Звіти автоматично оновляються при кожній зміні.

2. **Pre-commit Hook:**
   ```bash
   pre-commit install
   ```
   Звіти автоматично оновляються при кожному коміті.

3. **Windsurf Workflows:**
   ```
   Ctrl+L → /update-codemap
   Ctrl+L → /analyze-dead-code
   Ctrl+L → /detect-cycles
   Ctrl+L → /refactor-with-context
   ```

---

**Готово! Почни з [`QUICKSTART.md`](QUICKSTART.md) або запусти:**

```bash
python3 codemap_analyzer.py --once
```

Cascade матиме 100% контекст твого проєкту! 🚀
