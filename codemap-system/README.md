# 🔥 Codemap System – Complete Setup

## 🎯 Два файли для повного запуску

### 1️⃣ **DEPLOY_FINAL.sh** – Деплой системи

```bash
bash DEPLOY_FINAL.sh
```

**Що робить:**
- ✅ Перевіряє Python 3
- ✅ Перевіряє структуру папок
- ✅ Встановлює залежності (pip install)
- ✅ Налаштовує Windsurf конфіг (`~/.codeium/windsurf/mcp_config.json`)
- ✅ Тестує MCP Server (initialize + tools/list)

**Результат:**
```
✅ Система розгорнута
✅ Windsurf конфіг налаштовано
✅ MCP Server протестовано
```

---

### 2️⃣ **RUN_FULL.sh** – Запуск повного фулу

```bash
bash RUN_FULL.sh
```

**Що запускає:**
- ✅ **Enhanced Analyzer** (5 шарів, безперервна робота)
  - Шар 1: Виявлення мертвих файлів
  - Шар 2: Виявлення мертвих функцій
  - Шар 3: Побудова графу залежностей
  - Шар 4: Виявлення циклів і ізоляції
  - Шар 5: Аналіз якості і дублікатів

- ✅ **MCP Windsurf Server** (16 інструментів)
  - 6 базових інструментів
  - 7 advanced інструментів
  - 3 power tools (гіпер-інструменти)

- ✅ **Синхронізація з Windsurf**
  - Реальний час комунікація
  - Автоматичне оновлення звітів
  - Постійний аналіз

- ✅ **Автономна робота**
  - Безперервний цикл аналізу (кожну хвилину)
  - Автоматичне перезапускання
  - Самовідновлення при помилках

**Результат:**
```
✅ Analyzer запущено (PID: XXXX)
✅ MCP Server запущено (PID: XXXX)
✅ Синхронізовано з Windsurf
✅ 16 інструментів готові
✅ Постійний аналіз активний
```

---

## 📊 Повні можливості системи

### 🔍 Аналіз

| Можливість                | Файл                          | Статус   |
| ------------------------- | ----------------------------- | -------- |
| Виявлення мертвих файлів  | Analyzer (Шар 1)              | ✅ ACTIVE |
| Виявлення мертвих функцій | Analyzer (Шар 2)              | ✅ ACTIVE |
| Граф залежностей          | Analyzer (Шар 3)              | ✅ ACTIVE |
| Виявлення циклів          | Analyzer (Шар 4)              | ✅ ACTIVE |
| Аналіз якості             | Analyzer (Шар 5)              | ✅ ACTIVE |
| Аналіз дублікатів         | tools/duplication_analyzer.py | ✅ ACTIVE |

### 🛠️ Інструменти (16 всього)

**3 Power Tools (гіпер-інструменти):**
```
@cascade get_quick_assessment(directory: "orchestrator")
@cascade get_disqualification_report(directory: "orchestrator")
@cascade get_editor_quick_view(file_path: "orchestrator/app.js")
```

**7 Advanced Tools:**
```
@cascade analyze_file_deeply(file_path: "orchestrator/app.js")
@cascade compare_functions(file1, func1, file2, func2)
@cascade find_duplicates_in_directory(directory: "orchestrator")
@cascade analyze_impact(file_path: "orchestrator/app.js")
@cascade classify_files(directory: "orchestrator")
@cascade generate_refactoring_plan(priority: "high")
@cascade visualize_dependencies(file_path: "orchestrator/app.js", depth: 2)
```

**6 Basic Tools:**
```
@cascade get_layer_analysis(layer: 1)
@cascade get_dead_code_summary()
@cascade get_dependency_relationships(file_path: "orchestrator/app.js")
@cascade get_circular_dependencies()
@cascade get_quality_report(file_path: "orchestrator/app.js")
@cascade get_analysis_status()
```

### 🌐 Windsurf Синхронізація

- ✅ Реальний час комунікація через stdio
- ✅ Автоматичне оновлення конфігу
- ✅ Постійна синхронізація звітів
- ✅ Миттєві результати аналізу

### 🔄 Автономна робота

- ✅ Безперервний цикл аналізу (кожну хвилину)
- ✅ Автоматичне перезапускання при помилках
- ✅ Самовідновлення процесів
- ✅ Логування всіх операцій

---

## 🚀 Швидкий старт

### Крок 1: Деплой
```bash
cd /Users/dev/Documents/GitHub/atlas4/codemap-system
bash DEPLOY_FINAL.sh
```

### Крок 2: Перезавантажити Windsurf
```bash
# Cmd+Q на Mac (закрити)
# Потім відкрити Windsurf знову
```

### Крок 3: Запустити повний фул
```bash
bash RUN_FULL.sh
```

### Крок 4: Перевірити MCP у Windsurf
Ліва панель → MCP → orchestrator-codemap ✅

### Крок 5: Використовувати команди
```
@cascade get_quick_assessment(directory: "orchestrator")
```

---

## 📁 Структура папок

```
codemap-system/
├── 🚀 DEPLOY_FINAL.sh          ← ДЕПЛОЙ
├── 🚀 RUN_FULL.sh              ← ЗАПУСК
├── 📖 README.md                ← ЦЕЙ ФАЙЛ
│
├── 📁 core/                    ← ОСНОВНІ КОМПОНЕНТИ
│   ├── mcp_enhanced_analyzer.py    (5-шаровий аналізатор)
│   ├── mcp_enhanced_server.py      (MCP Server - 16 інструментів)
│   ├── mcp_windsurf_server.py      (Windsurf integration)
│   └── mcp_advanced_tools.py       (Advanced tools)
│
├── 📁 tools/                   ← ДОПОМІЖНІ ІНСТРУМЕНТИ
│   ├── windsurf_power_tools.py     (3 гіпер-інструменти)
│   ├── architecture_generator.py   (Генерація архітектури)
│   └── duplication_analyzer.py     (Аналіз дублікатів)
│
├── 📁 docs/                    ← ДОКУМЕНТАЦІЯ
│   ├── STRUCTURE.md
│   ├── ADVANCED_TOOLS_GUIDE.md
│   ├── MCP_INTEGRATION_GUIDE.md
│   └── ... (30+ файлів)
│
├── 📁 legacy/                  ← АРХІВОВАНІ ФАЙЛИ
├── 📁 logs/                    ← ЛОГИ
├── 📁 reports/                 ← ЗВІТИ
├── 📁 scripts/                 ← СТАРІ СКРИПТИ
├── 📁 tools/                   ← ДОПОМІЖНІ ІНСТРУМЕНТИ
└── 📁 example_project/         ← ПРИКЛАД
```

---

## 🔍 Перевірка статусу

### Чи Analyzer запущено?
```bash
ps aux | grep mcp_enhanced_analyzer | grep -v grep
```

### Чи MCP Server запущено?
```bash
ps aux | grep mcp_windsurf_server | grep -v grep
```

### Чи логи генеруються?
```bash
tail -f logs/analyzer.log
tail -f logs/mcp_windsurf_server.log
```

### Чи звіти оновлюються?
```bash
ls -lh reports/
```

---

## 🛑 Зупинення системи

### Через Ctrl+C
```
Ctrl+C (якщо запущено в терміналі)
```

### Вручну
```bash
pkill -f mcp_enhanced_analyzer
pkill -f mcp_windsurf_server
```

---

## 📊 Компоненти системи

### Core (основні)
| Файл                       | Функція                      | Статус   |
| -------------------------- | ---------------------------- | -------- |
| `mcp_enhanced_analyzer.py` | 5-шаровий аналізатор         | ✅ ACTIVE |
| `mcp_enhanced_server.py`   | MCP Server (16 інструментів) | ✅ ACTIVE |
| `mcp_windsurf_server.py`   | Windsurf integration         | ✅ ACTIVE |
| `mcp_advanced_tools.py`    | Advanced tools               | ✅ ACTIVE |

### Tools (допоміжні)
| Файл                        | Функція               | Статус   |
| --------------------------- | --------------------- | -------- |
| `windsurf_power_tools.py`   | 3 гіпер-інструменти   | ✅ ACTIVE |
| `architecture_generator.py` | Генерація архітектури | ✅ ACTIVE |
| `duplication_analyzer.py`   | Аналіз дублікатів     | ✅ ACTIVE |

---

## ✨ Результат

✅ **Повна функціональність** – Всі 5 шарів аналізу  
✅ **16 інструментів** – Готові до використання  
✅ **Windsurf синхронізація** – Реальний час  
✅ **Автономна робота** – Безперервний аналіз  
✅ **Чистий корінь** – Організована структура  
✅ **Документація** – У папці docs/  

---

## 📝 Документація

Вся документація знаходиться в папці `docs/`:
- `STRUCTURE.md` – Структура системи
- `ADVANCED_TOOLS_GUIDE.md` – Гайд по інструментам
- `MCP_INTEGRATION_GUIDE.md` – Інтеграція з Windsurf
- ... та 30+ інших файлів

---

**Status**: ✅ READY  
**Main Files**: 2 (DEPLOY_FINAL.sh + RUN_FULL.sh)  
**Active Components**: 7  
**Tools**: 16  
**Layers**: 5  

🔥 **СИСТЕМА ГОТОВА ДО ВИКОРИСТАННЯ!** 🔥
