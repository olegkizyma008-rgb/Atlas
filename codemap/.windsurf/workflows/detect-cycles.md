# 🔄 Detect Circular Dependencies

Знаходить циклічні залежності в проєкті.

## Кроки

1. **Запусти аналізатор**
   ```bash
   python3 codemap_analyzer.py --once
   ```

2. **Прочитай звіт про цикли**
   @reports/CODEMAP_SUMMARY.md

3. **Аналіз циклів**
   Cascade бачить всі циклічні залежності у форматі:
   ```
   module_a → module_b → module_c → module_a
   ```

4. **Рефакторинг**
   Cascade може запропонувати винести спільний код в окремий модуль.
