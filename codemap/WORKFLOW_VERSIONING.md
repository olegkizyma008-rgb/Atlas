# 🔄 Версіонування Workflows

Як система автоматично оновлює workflows при розгортанні.

## 🎯 Проблема

Коли ти копіюєш папку `codemap` в різні проєкти, старі workflows можуть конфліктувати з новими версіями. Це може привести до:
- ❌ Невідповідних версій workflows
- ❌ Помилок при запуску
- ❌ Змішування різних версій

## ✅ Рішення

Система автоматично:
1. **Перевіряє версію** workflows при розгортанні
2. **Оновлює** старі workflows до нової версії
3. **Зберігає** старі версії як резервну копію
4. **Уникає конфліктів** між версіями

## 📋 Як це працює

### Крок 1: Перевірка версії

При запуску `./deploy.sh`:

```bash
./deploy.sh
```

Система перевіряє файл `.windsurf/workflows/VERSION`:

```
.windsurf/workflows/VERSION
1.0.0
```

### Крок 2: Порівняння версій

```
Поточна версія: 1.0.0
Збережена версія: 0.9.0 (стара)

Версії не збігаються → оновлення потрібне!
```

### Крок 3: Резервне копіювання

Старі workflows зберігаються:

```
.windsurf/workflows/
├── update-codemap.md          # Нова версія
├── analyze-dead-code.md       # Нова версія
├── detect-cycles.md           # Нова версія
├── refactor-with-context.md   # Нова версія
├── VERSION                    # 1.0.0
└── .backup_v0.9.0_20251118_203726/
    ├── update-codemap.md      # Стара версія
    ├── analyze-dead-code.md   # Стара версія
    ├── detect-cycles.md       # Стара версія
    └── refactor-with-context.md  # Стара версія
```

### Крок 4: Оновлення версії

Файл `VERSION` оновлюється:

```
.windsurf/workflows/VERSION
1.0.0  # Оновлено
```

## 🔄 Сценарії використання

### Сценарій 1: Перший запуск

```bash
cp -r codemap /path/to/project
cd /path/to/project/codemap
./deploy.sh
```

Результат:
- ✅ Версія встановлена: 1.0.0
- ✅ Workflows готові
- ✅ Система розгорнута

### Сценарій 2: Копіювання старої версії

```bash
# Папка 1 (стара версія)
cp -r codemap_old /path/to/project1
cd /path/to/project1/codemap_old
./deploy.sh
```

Результат:
- ⚠️ Знайдена стара версія workflows: 0.9.0
- ✅ Старі workflows збережені в `.backup_v0.9.0_*/`
- ✅ Workflows оновлені до 1.0.0
- ✅ Система розгорнута

### Сценарій 3: Повторний запуск

```bash
./deploy.sh
```

Результат:
- ✅ Версія актуальна: 1.0.0
- ✅ Workflows не змінюються
- ✅ Система запущена в режимі спостереження

### Сценарій 4: Оновлення системи

Коли вийде нова версія (1.1.0):

```bash
# Завантаж нову версію
git pull origin main

# Запусти розгортання
./deploy.sh
```

Результат:
- ⚠️ Знайдена стара версія workflows: 1.0.0
- ✅ Старі workflows збережені в `.backup_v1.0.0_*/`
- ✅ Workflows оновлені до 1.1.0
- ✅ Система розгорнута

## 📊 Структура версіонування

### Файл VERSION

```
.windsurf/workflows/VERSION
1.0.0
```

Формат: `MAJOR.MINOR.PATCH`

- **MAJOR** — великі зміни (несумісні)
- **MINOR** — нові функції (сумісні)
- **PATCH** — виправлення помилок

### Резервні копії

```
.windsurf/workflows/.backup_v{VERSION}_{TIMESTAMP}/
```

Приклади:
- `.backup_v0.9.0_20251118_203726/`
- `.backup_v1.0.0_20251118_203800/`
- `.backup_v1.0.1_20251118_203830/`

## 🛡️ Захист від конфліктів

### Перед оновленням
```
Workflows версія: 0.9.0
Система версія: 1.0.0
❌ Конфлікт!
```

### Після оновлення
```
Workflows версія: 1.0.0
Система версія: 1.0.0
✅ Все збігається!
```

## 🚀 Як використовувати

### Запуск розгортання

```bash
./deploy.sh
```

Система автоматично:
1. Перевіряє версію workflows
2. Оновлює, якщо потрібно
3. Зберігає старі версії
4. Запускає систему

### Доступ до старих версій

```bash
# Переглянути всі резервні копії
ls -la .windsurf/workflows/.backup_*/

# Переглянути конкретну версію
cat .windsurf/workflows/.backup_v0.9.0_*/update-codemap.md

# Відновити стару версію
cp .windsurf/workflows/.backup_v0.9.0_*/* .windsurf/workflows/
```

### Перевірка версії

```bash
# Переглянути поточну версію
cat .windsurf/workflows/VERSION

# Перевірити версію системи
grep "current_version=" deploy.sh
```

## 📋 Логи оновлення

При запуску `./deploy.sh` ти побачиш:

```
▶ Перевіряю версію workflows...
ℹ Знайдена стара версія workflows: 0.9.0
ℹ Оновлюю до: 1.0.0
✅ Старі workflows збережені в: .windsurf/workflows/.backup_v0.9.0_20251118_203726
✅ Workflows оновлені до версії: 1.0.0
```

Або якщо версія актуальна:

```
▶ Перевіряю версію workflows...
✅ Workflows актуальні (версія 1.0.0)
```

## 🎯 Результат

### Перед версіонуванням
- ❌ Можуть бути конфлікти між версіями
- ❌ Неясно, яка версія workflows встановлена
- ❌ Складно відновити стару версію

### Після версіонування
- ✅ Версії автоматично синхронізуються
- ✅ Ясно, яка версія встановлена
- ✅ Легко відновити стару версію
- ✅ Немає конфліктів при копіюванні
- ✅ Система завжди актуальна

## 📞 Поради

### Порада 1: Регулярно очищуй резервні копії

```bash
# Видали старі резервні копії (старше 30 днів)
find .windsurf/workflows/.backup_* -type d -mtime +30 -exec rm -rf {} \;
```

### Порада 2: Архівуй важливі версії

```bash
# Архівуй поточну версію workflows
tar -czf workflows_backup_v1.0.0_$(date +%Y%m%d).tar.gz .windsurf/workflows/
```

### Порада 3: Відслідковуй версії в Git

```bash
# Додай VERSION файл в Git
git add .windsurf/workflows/VERSION
git commit -m "Update workflows to version 1.0.0"
```

## 🎉 Готово!

Система тепер:
- ✅ Автоматично оновлює workflows
- ✅ Зберігає старі версії
- ✅ Уникає конфліктів
- ✅ Синхронізує версії

```bash
./deploy.sh
```

Workflows завжди будуть актуальні! 🚀

---

**Дякуємо за використання Codemap Analyzer!**
