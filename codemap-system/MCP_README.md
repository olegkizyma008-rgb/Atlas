# 🌐 MCP Server - Управління інструментами в Cascade

## Що це?

**MCP** (Model Context Protocol) — це протокол, який дозволяє AI-асистентам (як Cascade) керувати зовнішніми інструментами без явного запуску сервера.

## Як це працює?

### Без MCP (старий спосіб)
```
Ви → Cascade → "Я не можу це зробити"
```

### З MCP (новий спосіб)
```
Ви → Cascade → MCP Server → Інструменти → Результат → Cascade → Ви
```

## 🚀 Швидкий старт

### 1. Перевірити конфігурацію
```bash
cat ~/.codeium/windsurf/mcp_config.json
```

Повинна бути така конфігурація:
```json
{
  "mcpServers": {
    "codemap": {
      "command": "python3",
      "args": ["/Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_server.py"],
      "env": {
        "PYTHONPATH": "/Users/dev/Documents/GitHub/atlas4/codemap-system",
        "PROJECT_ROOT": "/Users/dev/Documents/GitHub/atlas4",
        "PYTHONUNBUFFERED": "1"
      }
    }
  }
}
```

### 2. Протестувати сервер
```bash
cd /Users/dev/Documents/GitHub/atlas4/codemap-system
./test_mcp_server.sh
```

### 3. Перезавантажити Windsurf
- Закрийте Windsurf
- Відкрийте знову
- Тепер MCP сервер буде активний

## 📚 Доступні інструменти

### 1. Аналіз файлу
```
@cascade analyze_file_deeply(file_path: "path/to/file.js")
```

### 2. Пошук дублікатів
```
@cascade find_duplicates(directory: "src")
```

### 3. Аналіз залежностей
```
@cascade analyze_dependencies(file_path: "src/app.js")
```

### 4. Пошук мертвого коду
```
@cascade find_dead_code(directory: "src")
```

### 5. План рефакторингу
```
@cascade generate_refactoring_plan(priority: "high")
```

### 6. Резюме проекту
```
@cascade get_project_summary()
```

## 🔧 Архітектура

```
┌──────────────────────────────────────────────────────────┐
│ Windsurf IDE                                             │
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │ Cascade (AI Assistant)                             │  │
│ │ "Проаналізуй файл app.js"                          │  │
│ └────────────────┬─────────────────────────────────┘  │
│                  │                                      │
│                  ↓                                      │
│ ┌────────────────────────────────────────────────────┐  │
│ │ MCP Config Reader                                  │  │
│ │ Читає ~/.codeium/windsurf/mcp_config.json          │  │
│ └────────────────┬─────────────────────────────────┘  │
│                  │                                      │
│                  ↓                                      │
│ ┌────────────────────────────────────────────────────┐  │
│ │ Process Manager                                    │  │
│ │ Запускає: python3 mcp_server.py                    │  │
│ └────────────────┬─────────────────────────────────┘  │
│                  │                                      │
│                  ↓                                      │
│ ┌────────────────────────────────────────────────────┐  │
│ │ JSON-RPC Protocol Handler                          │  │
│ │ Надсилає запити через stdin/stdout                 │  │
│ └────────────────┬─────────────────────────────────┘  │
└────────────────┼──────────────────────────────────────┘
                 │
                 ↓
    ┌────────────────────────────┐
    │ mcp_server.py              │
    │ (MCP Server Process)       │
    │                            │
    │ Інструменти:               │
    │ - analyze_file_deeply()    │
    │ - find_duplicates()        │
    │ - analyze_dependencies()   │
    │ - find_dead_code()         │
    │ - generate_refactoring()   │
    │ - get_project_summary()    │
    │                            │
    └────────────────────────────┘
```

## 🔍 Як Cascade керує інструментами

### Процес:

1. **Ви ставите завдання**
   ```
   "Проаналізуй файл orchestrator/app.js"
   ```

2. **Cascade обирає інструмент**
   ```
   "Мені потрібен analyze_file_deeply"
   ```

3. **Windsurf запускає MCP сервер**
   ```bash
   python3 /path/to/mcp_server.py
   ```

4. **Windsurf надсилає JSON запит**
   ```json
   {
     "jsonrpc": "2.0",
     "id": 1,
     "method": "tools/call",
     "params": {
       "name": "analyze_file_deeply",
       "arguments": {"file_path": "orchestrator/app.js"}
     }
   }
   ```

5. **Сервер обробляє запит**
   ```python
   def analyze_file_deeply(self, file_path):
       # Аналізує файл
       return результат
   ```

6. **Сервер повертає результат**
   ```json
   {
     "jsonrpc": "2.0",
     "id": 1,
     "result": {
       "content": [{"type": "text", "text": "...результат..."}]
     }
   }
   ```

7. **Cascade отримує результат**
   ```
   "Ось аналіз файлу..."
   ```

## 🛠️ Налаштування

### Локальна конфігурація (для цього проекту)
```
~/.codeium/windsurf/mcp_config.json
```

### Глобальна конфігурація (для всіх проектів)
```
~/.codeium/windsurf/mcp_config.json
```

## ✅ Перевірка статусу

### Тест 1: Ініціалізація
```bash
echo '{"jsonrpc": "2.0", "id": 1, "method": "initialize", "params": {}}' | \
  python3 /Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_server.py
```

Очікуваний результат:
```json
{"jsonrpc": "2.0", "id": 1, "result": {"protocolVersion": "2024-11-05", ...}}
```

### Тест 2: Список інструментів
```bash
echo '{"jsonrpc": "2.0", "id": 2, "method": "tools/list", "params": {}}' | \
  python3 /Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_server.py
```

Очікуваний результат:
```json
{"jsonrpc": "2.0", "id": 2, "result": {"tools": [...]}}
```

### Тест 3: Виклик інструменту
```bash
echo '{"jsonrpc": "2.0", "id": 3, "method": "tools/call", "params": {"name": "get_project_summary", "arguments": {}}}' | \
  python3 /Users/dev/Documents/GitHub/atlas4/codemap-system/mcp_server.py
```

## 🎯 Ключові моменти

### ✅ Що працює
- Cascade автоматично запускає MCP сервер
- Не потрібно вручну запускати сервер
- Код виконується локально (безпечно)
- Немає затримок мережі
- Можна додавати нові інструменти

### ⚠️ Що потрібно знати
- Windsurf повинен бути перезавантажений після зміни конфігурації
- Сервер запускається як subprocess (автоматично)
- Кожен запит — це новий JSON-RPC виклик
- Результати повертаються як текст

## 📝 Додавання нових інструментів

Див. `MCP_SETUP_GUIDE.md` для детальної інструкції.

## 🚀 Наступні кроки

1. ✅ Перезавантажити Windsurf
2. ✅ Протестувати інструменти
3. ✅ Використовувати в Cascade
4. ✅ Додавати нові інструменти за потребою

## 📞 Допомога

Якщо щось не працює:

1. Перевірте конфігурацію:
   ```bash
   cat ~/.codeium/windsurf/mcp_config.json
   ```

2. Протестуйте сервер:
   ```bash
   ./test_mcp_server.sh
   ```

3. Перезавантажте Windsurf

4. Спробуйте ще раз

---

**Готово!** Тепер Cascade може керувати інструментами аналізу коду! 🎉
