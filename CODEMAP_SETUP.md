# CodeMap Automatic Sync Setup

Щоб синхронізувати кодемеп з Cascade по дефолту при запуску, виконайте наступні кроки:

## 1. Ініціалізація кодемеп

Запустіть скрипт ініціалізації:

```bash
.windsurf/init-codemap.sh
```

Це створить перший аналіз коду в `codemap-system/reports/codemap_analysis.json`.

## 2. MCP Сервер вже налаштований

MCP сервер автоматично налаштований у `.windsurf/workspace.code-workspace`:

```json
"mcp": {
    "servers": {
        "codemap": {
            "command": "python3",
            "args": [
                "codemap-system/mcp_codemap_server.py"
            ]
        }
    }
}
```

Це означає, що при запуску Windsurf/Cascade, MCP сервер автоматично запуститься.

## 3. Перезавантажити Windsurf

Перезавантажте Windsurf, щоб MCP сервер почав працювати.

## 4. Оновлення кодемеп

Щоб оновити кодемеп вручну, використовуйте команду:

```bash
/update-codemap
```

Або запустіть скрипт напряму:

```bash
cd codemap-system
python3 codemap_analyzer.py
```

## 5. Автоматичний контекст перед кожною задачею

Щоб Cascade автоматично отримував контекст перед кожною задачею, можна використовувати `cascade_pre_task_hook.py`:

```bash
python3 codemap-system/cascade_pre_task_hook.py --mode context
```

Це покаже:
- 📊 Огляд проекту
- ⚠️ Проблеми якості
- 🎯 Можливості рефакторингу
- 📊 Метрики складності
- 💡 Рекомендації

## Як це працює

1. **MCP Сервер** запускається автоматично при запуску Windsurf
2. **CodeMap аналіз** зберігається в `codemap-system/reports/codemap_analysis.json`
3. **Cascade** може отримати доступ до аналізу через MCP сервер
4. **Контекст** автоматично інжектується перед кожною задачею

## Перевірка статусу

Щоб перевірити, чи кодемеп синхронізований:

```bash
ls -la codemap-system/reports/codemap_analysis.json
```

Якщо файл існує, кодемеп готовий до використання.

## Проблеми та рішення

### MCP сервер не запускається
- Перевірте, що Python 3 встановлений: `python3 --version`
- Перевірте, що `codemap-system/mcp_codemap_server.py` існує
- Перезавантажте Windsurf

### Аналіз не оновлюється
- Запустіть `/update-codemap` вручну
- Перевірте права доступу до `codemap-system/reports/`

### Контекст не інжектується
- Переконайтесь, що `codemap_analysis.json` існує
- Запустіть `cascade_pre_task_hook.py --mode context` для перевірки
